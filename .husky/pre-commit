export PATH="/opt/homebrew/bin:/usr/local/opt/node@24/bin:$PATH"

# Run Tech Integrity Check (CRITICAL)
npm run verify:tech-integrity

# Run Type Checking
npm run typecheck

# Run Linting & Formatting (only on staged files)
npx lint-staged

# Security Checks
./scripts/security/check-secrets.sh
