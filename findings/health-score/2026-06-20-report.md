# RUN Remix - System-Wide Health Score

**Date:** 2026-06-20
**Mode:** Read-only baseline audit

## Composite Score: 70 / 100 (Grade: C)
*Baseline calculated using B.L.A.S.T. heuristic scoring rules.*

---

## Dimension Breakdown

### 1. Security & Compliance (23/25)
* **Status:** Passed with warnings
* **Findings:**
  - `check:audit` flagged `undici` and `nodemailer` with known CVEs. (Matches known Phase 4/5 tracking).
  - CSRF and `httpOnly` sessions are correctly configured.
  - Rate Limiting logic correctly implemented with In-Memory fallback.
  - Zod validation and `opossum` circuit breakers are in active use across services.

### 2. Architecture & Hard Rules Compliance (0/20)
* **Status:** Failed (Critical Regressions)
* **Findings:**
  - **H04 (`onSubmit` instead of `action`):** 5 violations found. Forms should use React 19 `action`.
  - **H12/H13 (`try/catch` in routes):** 5 instances of `try` blocks found inside route files (D02 baseline is 4; +1 excess).
  - **D04 (Local Zod Schemas):** 23 inline schema declarations (`z.object`) found outside of `shared/` (Baseline is 15; +8 excess).
  - **Passes:** H01 (`process.env.PORT || 3000`), H02 (`forwardRef`), H03 (default exports).

### 3. Code Quality (11.4/15)
* **Status:** Medium
* **Findings:**
  - `verify:tech-integrity` flagged 8 typecheck and biome errors (untyped `any`, formatting, etc).
  - `knip` reported unused exported types and dead code.

### 4. Test Coverage & Reliability (12.6/15)
* **Status:** Failed to Execute
* **Findings:**
  - Unit tests failed execution (`exit code 1`) due to `REDIS_URL` missing, causing a backend boot crash.
  - Playwright E2E and A11y tests cannot run locally due to missing browser binaries.

### 5. Performance (9.4/10)
* **Status:** Good
* **Findings:**
  - `lhci` script missing, so no automated lighthouse performance tests ran.
  - Main Javascript bundle (`dist/index.js`) sits at 1.3MB (`check:bundle` warning for chunk > 500kb).
  - Caching layer (`lru-cache` & `ioredis`) is implemented and instantiated.

### 6. Build & CI Health (9.2/10)
* **Status:** Minor Issues
* **Findings:**
  - `npm run verify:tech-integrity` fails at the typecheck and formatting steps.
  - `check:secrets` passes with zero leaked secrets.

### 7. Accessibility (4.85/5)
* **Status:** Assumed Good (Unverified)
* **Findings:**
  - No automated A11y tests ran due to the Playwright environment issue.

---

## Current Sprint / Phase Alignment
- **Detected Phase Status:** DISCOVERY / DEBT REDUCTION
- **CVE Status:** The two ship-blocking CVEs (`undici`, `nodemailer`) are still present in `package.json` dependency graphs.

## Prioritized Remediation Roadmap (B.L.A.S.T.)

1. **[P0 - Security] Resolve CVEs**
   - Address the `undici` and `nodemailer` vulnerabilities blocking shipment.
2. **[P0 - Environment] Fix Test Suite Execution**
   - Provide a mock `REDIS_URL` or run a local redis container to allow `vitest` to boot the server and collect coverage.
   - Run `npx playwright install` for UI/E2E testing.
3. **[P1 - Architecture] React 19 Forms & Error Handling**
   - Refactor the 5 `onSubmit` usages to React 19 `<form action={fn}>`.
   - Remove the excess `try/catch` block in `server/routes/` and delegate to the `neverthrow` service tier.
4. **[P1 - Architecture] Centralize Zod Schemas**
   - Move the 8 excess `z.object` declarations from `server/` into `@run-remix/shared/schemas/`.
5. **[P2 - Code Quality] Fix Biome & TS Errors**
   - Run `npm run format` and `npm run lint:fix` to clear the 8 type/linting errors preventing a clean `verify:tech-integrity` run.
6. **[P2 - Performance] Code Splitting**
   - Split up the 1.3MB `index.js` chunk using Vite/Rolldown dynamic imports to satisfy the `< 500kb` hard constraint.
