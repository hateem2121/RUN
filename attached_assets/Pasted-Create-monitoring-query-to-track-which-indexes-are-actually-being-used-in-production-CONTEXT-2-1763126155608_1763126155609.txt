Create monitoring query to track which indexes are actually being used in production:

CONTEXT:

- 25 indexes exist, need to verify all are utilized

- Some indexes may be redundant or unused

- PostgreSQL tracks index usage in pg_stat_user_indexes

TASK:

1. Create server/scripts/monitor-index-usage.ts

2. Add query to check unused indexes:

   const unusedIndexes = await db.execute(sql`

     SELECT 

       schemaname,

       tablename,

       indexname,

       idx_scan as scan_count,

       pg_size_pretty(pg_relation_size(indexrelid)) as index_size

     FROM pg_stat_user_indexes

     WHERE schemaname = 'public'

       AND idx_scan = 0

       AND indexrelname NOT LIKE '%_pkey'

     ORDER BY pg_relation_size(indexrelid) DESC;

   `);

3. Add query to check index usage statistics:

   (Show top 10 most-used and least-used indexes)

4. Add to server/middleware/enhanced-health.ts:

   - Include index usage stats in /health/detailed endpoint

   - Flag indexes with 0 scans as warnings

CONSTRAINTS:

- Do NOT remove any indexes in this task (only monitor)

- Query must be read-only (no modifications)

- Add to health check, not as scheduled job

- Follow existing health check pattern in file

SUCCESS CRITERIA:

- Monitoring script created

- Query identifies indexes with 0 scans

- Health endpoint includes index usage metrics

- Output format matches existing health check structure

---

Run EXPLAIN ANALYZE on critical queries to verify optimizations are working:

CONTEXT:

- Need to confirm new indexes are being used

- Verify query consolidation reduced execution time

- Ensure no unexpected sequential scans

TASK:

1. Create server/scripts/validate-query-plans.ts

2. Add EXPLAIN ANALYZE for these queries:

   - getProductByPath (main target for optimization)

   - getProductsSummary (hot path)

   - getMediaAssets with search filter (uses trigram indexes)

   - getProductsByTag (uses new GIN index)

3. For each query, capture:

   - Planning time

   - Execution time

   - Indexes used

   - Sequential scans (should be 0 for indexed columns)

   - Total rows scanned

4. Output comparison:

   BEFORE: [baseline from audit]

   AFTER: [current execution plan]

   IMPROVEMENT: [percentage change]

CONSTRAINTS:

- Run on test database first (not production)

- Use realistic test data (minimum 100 products, 50 media assets)

- Do NOT run UPDATE/INSERT queries with EXPLAIN ANALYZE

- Add timeout protection (max 10 seconds per query)

SUCCESS CRITERIA:

- Script executes all 4 EXPLAIN ANALYZE queries

- Output shows indexes are being used (no Seq Scan on indexed columns)

- getProductByPath shows query count reduction (9 → 7)

- Execution times documented for comparison

- No errors or timeouts

---

Create automated tests to prevent future performance regressions:

CONTEXT:

- Need to ensure optimizations don't break functionality

- Catch performance degradation before production

- Validate column selection changes return expected data

TASK:

1. Create server/tests/performance-regression.test.ts

2. Add test cases for:

   a. getProducts() returns correct 7 columns (PRODUCT_CARD_COLUMNS)

   b. getProductByPath() returns complete product context

   c. getMediaAssets() returns correct 8 columns (MEDIA_GRID_COLUMNS)

   d. Query execution time benchmarks (baseline vs optimized)

3. Test structure for each:

   - Arrange: Seed test data

   - Act: Execute query

   - Assert: 

     * Verify expected columns present

     * Verify no extra columns returned

     * Verify execution time under threshold

4. Add to existing test suite (search for test file pattern in server/)

CONSTRAINTS:

- Use existing test framework (check server/tests/ for pattern)

- Do NOT create new test infrastructure

- Tests must be deterministic (seed data first)

- Mock external dependencies (cache, circuit breaker)

SUCCESS CRITERIA:

- 12+ test cases covering all optimized queries

- Tests pass with optimized code

- Execution time assertions reasonable (95th percentile)

- No flaky tests (run 3 times to verify)