Reduce over-fetching in product listing queries by creating lightweight column set:

CONTEXT:

- getProducts() fetches 22 columns via PRODUCT_SUMMARY_COLUMNS

- Product cards only use 7 columns (68% waste)

- Fields like imageIds, videos, technicalSpecs only needed in detail pages

TASK:

1. Open server/lib/repositories/product-repository.ts

2. Locate PRODUCT_SUMMARY_COLUMNS constant (around line 55)

3. Create NEW constant PRODUCT_CARD_COLUMNS above it with only:

   - id, name, slug, description, primaryImageId, categoryId, isFeatured

4. Find getProducts() function (line ~154)

5. Change .select(PRODUCT_SUMMARY_COLUMNS) to .select(PRODUCT_CARD_COLUMNS)

6. Find getProductsByCategory() function (line ~787)

7. Change .select(PRODUCT_SUMMARY_COLUMNS) to .select(PRODUCT_CARD_COLUMNS)

8. Do NOT modify getProductsSummary() - keep PRODUCT_SUMMARY_COLUMNS for backward compatibility

CONSTRAINTS:

- Do NOT remove PRODUCT_SUMMARY_COLUMNS constant (still used elsewhere)

- Do NOT modify getProductsSummary() function

- Only change these 2 functions: getProducts() and getProductsByCategory()

- Verify column names match exact schema field names

SUCCESS CRITERIA:

- PRODUCT_CARD_COLUMNS constant created with 7 fields

- getProducts() uses new column set

- getProductsByCategory() uses new column set

- getProductsSummary() unchanged (uses PRODUCT_SUMMARY_COLUMNS)

---

Reduce over-fetching in media asset listing queries:

CONTEXT:

- getMediaAssets() uses SELECT * (fetches 25 columns)

- Admin grid only uses 8 columns (52% waste)

- Fields like imageVariants, metadata, storagePath not displayed in grid

TASK:

1. Open server/lib/repositories/media-repository.ts

2. Find getMediaAssets() function (around line 79)

3. Before the function, add constant MEDIA_GRID_COLUMNS:

   const MEDIA_GRID_COLUMNS = {

     id: mediaAssets.id,

     url: mediaAssets.url,

     thumbnailUrl: mediaAssets.thumbnailUrl,

     filename: mediaAssets.filename,

     type: mediaAssets.type,

     mimeType: mediaAssets.mimeType,

     fileSize: mediaAssets.fileSize,

     createdAt: mediaAssets.createdAt,

   } as const;

4. Find line: db.select().from(mediaAssets)

5. Change to: db.select(MEDIA_GRID_COLUMNS).from(mediaAssets)

6. Do NOT modify getMediaAsset() function (single asset detail needs all fields)

CONSTRAINTS:

- Only modify getMediaAssets() function

- Do NOT touch getMediaAsset() (singular)

- Do NOT modify getMediaAssetsWithCount() - it calls getMediaAssets()

- Preserve all existing filter logic unchanged

SUCCESS CRITERIA:

- MEDIA_GRID_COLUMNS constant created with 8 fields

- getMediaAssets() uses explicit column selection instead of SELECT *

- getMediaAsset() unchanged (still fetches all columns)

- All filter conditions still work