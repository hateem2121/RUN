Map all database query patterns in the codebase:

1. Scan ALL repository files (*-repository.ts) and document:

   - Each function's query pattern (single query, parallel queries, sequential queries)

   - Number of database round-trips per function

   - Use of Promise.all vs sequential awaits

   

2. For getProductByPath specifically, trace the EXACT execution flow:

   - List each query in order with file location

   - Identify if queries run in parallel or waterfall

   - Document any existing JOIN patterns already used

   

3. Check if query batching utilities exist:

   - Search for: "batch", "bulkGet", "inArray", "whereIn"

   - Document any existing helper functions for batch queries

   

4. Output: Create a visual map (markdown table) showing:

   - Function name | Query count | Execution pattern | File location

SUCCESS CRITERIA: Complete inventory of all query patterns with zero assumptions about code structure.

---

Identify what optimization patterns ALREADY exist in the codebase:

1. Search for existing query consolidation patterns:

   - Look for JOIN usage in Drizzle queries (.leftJoin, .innerJoin, .rightJoin)

   - Find any queries using "select with relations" patterns

   - Document where batching with IN clauses is already implemented

   

2. Check for partial implementations:

   - Search comments containing: "TODO", "FIXME", "optimize", "performance"

   - Find any abandoned or half-implemented optimization attempts

   - Identify if cursor pagination exists anywhere (search: "cursor", "keyset")

   

3. Review utility files:

   - Check db/ or lib/ directories for query helper functions

   - Look for any caching decorators or query result transformers

   - Document any database middleware or query interceptors

   

4. Output: List all EXISTING optimization patterns with file paths and usage examples.

SUCCESS CRITERIA: Zero duplication risk - know what's already there before adding anything new.

---

Analyze data dependencies and flow for performance-critical endpoints:

1. For getProductByPath, trace data flow:

   - Which downstream queries DEPEND on the main product query result?

   - Which queries can run in parallel vs must run sequentially?

   - Identify if any data is fetched but never used in the response

   

2. Check for N+1 query patterns:

   - Find loops that contain database queries

   - Search for: ".map(async", ".forEach(async", "for...of" with await inside

   - Document each instance with line numbers and context

   

3. Analyze data transformation overhead:

   - Find where raw DB results are transformed/serialized

   - Check if transformations happen before or after queries complete

   - Identify any unnecessary data reshaping steps

   

4. Output: Dependency graph showing which queries block others and which are independent.

SUCCESS CRITERIA: Clear understanding of what can be parallelized vs what must be sequential.

---

Comprehensive audit of all pagination implementations:

1. Document ALL pagination patterns found (not just OFFSET):

   - Search for: "OFFSET", "offset(", "skip(", "cursor", "page"

   - List each with: endpoint path, query pattern, expected data volume

   

2. For each OFFSET usage, determine:

   - Is it user-facing (public) or admin-only?

   - What's the realistic max OFFSET value (based on data volume)?

   - Are there indexes supporting the ORDER BY clause?

   

3. Check for existing cursor/keyset implementations:

   - Search codebase for "cursor", "keyset", "seek"

   - Check API response types for cursor fields

   - Review frontend pagination components for cursor support

   

4. Analyze pagination consistency:

   - Do all endpoints use the same pagination pattern?

   - Check for hardcoded LIMIT values vs configurable limits

   

5. Output: Complete pagination inventory with risk assessment per endpoint.

SUCCESS CRITERIA: Know exact scope of pagination refactor needed.

---

Map how cache layers integrate with query execution:

1. Trace cache lookup flow:

   - Find all cache.get() calls BEFORE database queries

   - Document cache key patterns used (search: "cache.set", "cache.get")

   - Identify if cache checks happen at repository or service layer

   

2. Analyze cache-database coordination:

   - Find where cache is invalidated after writes

   - Check for cache stampede protection (multiple requests for same key)

   - Look for any cache warming strategies

   

3. Review cache hit/miss handling:

   - Search for fallback logic when cache misses

   - Check if queries are optimized differently for cache misses

   - Identify if partial caching is used (e.g., cache category, fetch products)

   

4. Document cache bypass scenarios:

   - Find queries that skip cache intentionally

   - Check for cache-control headers or force-refresh logic

   

5. Output: Cache flow diagram showing where each query layer checks/updates cache.

SUCCESS CRITERIA: Understand if query consolidation will break existing cache strategy.

---

Verify that existing indexes are actually being used by queries:

1. Cross-reference query patterns with index definitions:

   - For each slow query, confirm matching index exists

   - Check if WHERE clause columns match index column order

   - Verify ORDER BY columns are covered by indexes

   

2. Identify potential index mismatches:

   - Find queries filtering on multiple columns without composite index

   - Look for function-based filters (LOWER, DATE) that can't use indexes

   - Check for type mismatches (e.g., string compared to number)

   

3. Document index hints in code:

   - Search for: "USE INDEX", "FORCE INDEX", comments mentioning indexes

   - Check if Drizzle query builder has any index hint syntax used

   

4. Review conditional query patterns:

   - Find queries with dynamic WHERE clauses based on input

   - Check if different code paths use different indexes

   

5. Output: Matrix of queries vs indexes showing utilization gaps.

SUCCESS CRITERIA: Confirm no "invisible" index problems causing sequential scans.

---

Assess database resilience patterns and error handling:

1. Map circuit breaker implementation:

   - Find circuit breaker configuration (thresholds, timeout, reset)

   - Document which queries are protected vs unprotected

   - Check if circuit breaker state is shared across requests or per-connection

   

2. Analyze query timeout handling:

   - Search for query timeout configurations

   - Find how timeouts are handled (retry, fail, fallback)

   - Check for transaction timeout settings

   

3. Review connection pool management:

   - Document connection pool size and queue limits

   - Check for connection leak prevention (proper cleanup in finally blocks)

   - Verify pool exhaustion handling

   

4. Identify retry logic:

   - Find exponential backoff implementations

   - Check for idempotency guarantees on retried queries

   - Document which errors trigger retries vs immediate failures

   

5. Output: Resilience architecture diagram with all safety mechanisms mapped.

SUCCESS CRITERIA: Ensure optimization changes don't bypass existing safety rails.

---

Identify any existing denormalized data or computed columns:

1. Scan schema for denormalization patterns:

   - Find columns containing duplicated data from related tables

   - Search for JSONB columns storing nested/aggregated data

   - Look for "_cached" or "_computed" column naming patterns

   

2. Check for materialized views or summary tables:

   - Search schema files for: "VIEW", "MATERIALIZED", "_summary", "_rollup"

   - Document any tables used solely for read optimization

   

3. Analyze data consistency maintenance:

   - Find triggers or hooks that update denormalized data

   - Check for batch jobs that rebuild computed values

   - Look for "sync" or "rebuild" functions in background jobs

   

4. Review trade-offs already made:

   - Find comments explaining why denormalization was chosen

   - Check git history for denormalization-related commits

   - Document which writes are slower due to denormalization

   

5. Output: Inventory of existing denormalization with consistency mechanisms.

SUCCESS CRITERIA: Avoid proposing denormalization that's already implemented or was rejected.

----

Analyze what data is actually needed by API consumers:

1. Map API endpoint to response structure:

   - For each performance-critical endpoint, document FULL response schema

   - Compare query data fetched vs data actually returned in API response

   - Identify any fields fetched from DB but not included in response

   

2. Check for over-fetching patterns:

   - Find SELECT * queries where only specific columns are used

   - Look for nested data loaded but not always needed (e.g., media arrays)

   - Identify optional fields that could be lazy-loaded

   

3. Analyze field-level access patterns:

   - Review frontend code to see which response fields are actually used

   - Check if any fields are only used in specific UI states (tabs, modals)

   - Document any GraphQL-like field selection patterns already implemented

   

4. Review versioning and backwards compatibility:

   - Check for API version indicators in responses

   - Find deprecated fields still being fetched for compatibility

   

5. Output: Gap analysis showing fetched vs used data per endpoint.

SUCCESS CRITERIA: Identify opportunities to reduce query scope without breaking API contracts.

---

Assess how background jobs interact with query-heavy endpoints:

1. Inventory all background jobs:

   - Find cron jobs, scheduled tasks, queue workers

   - Document which jobs perform bulk database operations

   - Check job scheduling (do they overlap with peak traffic?)

   

2. Analyze write contention:

   - Find bulk INSERT/UPDATE operations that might lock tables

   - Check for VACUUM or REINDEX operations in cron

   - Identify any data import/sync jobs from external sources

   

3. Review cache invalidation jobs:

   - Find jobs that clear cache (e.g., nightly cache purge)

   - Check if cache warming happens after invalidation

   - Document any race conditions between cache updates and reads

   

4. Check database migration impact:

   - Review recent migration files for schema changes

   - Find any migrations that add indexes without CONCURRENTLY

   - Check for data backfill scripts that run during deploy

   

5. Output: Timeline showing when background jobs impact database performance.

SUCCESS CRITERIA: Ensure query optimizations account for background job load patterns.