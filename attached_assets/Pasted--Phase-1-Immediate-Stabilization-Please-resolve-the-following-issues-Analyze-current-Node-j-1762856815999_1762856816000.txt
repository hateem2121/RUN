**Phase 1: Immediate Stabilization**

Please resolve the following issues:

- Analyze current Node.js memory/heap usage and tune `--max-old-space-size` flags as needed. Trigger heap snapshot and set up automated alerts for memory thresholds and garbage collection stalls.
- Deduplicate all redundant product/category DB queries; identify unnecessary counts and move count aggregations for product listings to a cache, invalidating only when products change.
- Raise cache TTLs for static data such as navigation and homepage. Target minimum 70% cache hit rate. Identify and fix sources of cache misses.
- Validate that all real-time alerts (memory, query latency) are working and integrated into the dashboard/email system.
- Summarize all immediate steps taken and issues resolved in a brief report.
---
**Phase 2: Performance Optimizations**

Please implement the following optimizations:

- Refactor cache warmup logic to parallelize requests using safe batching (Promise.all or concurrency limits), reducing overall application startup time.
- Implement stale-while-revalidate caching for all static or rarely changed content, so users always receive a fast response even during cache refreshes.
- Audit and batch all DB subqueries using Promise.all, ensuring no sequential fetches for independent resources. Profile and summarize any batch time improvements.
- Audit media and high-throughput batch endpoints to ensure no excessive memory retention or unbuffered response streaming issues.
- Provide a detailed performance optimization report with before/after metrics.
---
**Phase 3: Deep Query/Cost Audit & Scaling**

Please execute the following audit and improvement steps:

- Run `EXPLAIN ANALYZE` on all slow Neon PostgreSQL queries, validate index coverage, and recommend or apply reindexing or index additions.
- Perform bloat check and automate periodic index/table maintenance for Neon PostgreSQL.
- Identify and propose setup/read-replica configuration if database load and cost warrant.
- Refactor all large product listings and batch operations to use cursor-based pagination, reducing query time and memory load.
- Summarize all database changes, cost optimizations, and scaling preparations in a copyable audit report.
