Create migration to drop unused columns that are never populated or read:

CONTEXT:

- categories.product_count: Never updated (COUNT queries used instead)

- products.category_path: Never populated (client-side path computation used)

- media_assets.download_count: Never updated (tracking not implemented)

- media_assets.last_accessed_at: Never updated (tracking not implemented)

- media_assets.size: Duplicate of file_size column

TASK:

1. Create migrations/cleanup/001_remove_dead_columns.sql

2. Add these DROP COLUMN statements:

   ALTER TABLE categories DROP COLUMN IF EXISTS product_count;

   ALTER TABLE products DROP COLUMN IF EXISTS category_path;

   ALTER TABLE media_assets DROP COLUMN IF EXISTS download_count;

   ALTER TABLE media_assets DROP COLUMN IF EXISTS last_accessed_at;

   ALTER TABLE media_assets DROP COLUMN IF EXISTS size;

3. Add rollback commands in comments:

   -- ROLLBACK:

   -- ALTER TABLE categories ADD COLUMN product_count INTEGER DEFAULT 0;

   -- (etc.)

4. Update shared/schema.ts:

   - Remove column definitions for these 5 fields

   - Add comments explaining removal with date

CONSTRAINTS:

- Do NOT drop columns used in any queries (verify with grep first)

- Add IF EXISTS to prevent errors if column already removed

- Document each column removal reason in migration comments

- Keep migration separate from other changes

SUCCESS CRITERIA:

- Migration file created with 5 DROP COLUMN statements

- All use IF EXISTS

- Rollback commands documented

- Schema file updated to remove definitions

- No errors when running grep for removed column names in src/



---

Document deprecated fields and migration timeline in schema:

CONTEXT:

- Some fields exist for backward compatibility but should be sunset

- fabrics.composition: Deprecated, use compositions array instead

- products.relatedProductIds: Replaced by context.categoryProducts

TASK:

1. Open shared/schema.ts

2. For each deprecated field, add JSDoc comment above definition:

   /**

    * @deprecated Use compositions array instead

    * @since 2024-01

    * @remove 2025-Q2

    */

   composition: varchar("composition", { length: 500 }).default(""),

3. Add deprecation warnings for:

   - fabrics.composition → Use compositions array

   - products.relatedProductIds → Use categoryProducts from context

   - Any other rarely-used fields identified in audit

4. Create docs/schema-deprecations.md:

   - List all deprecated fields

   - Explain replacement approach

   - Set removal timeline (6 months minimum)

CONSTRAINTS:

- Do NOT remove any columns in this task

- Do NOT modify queries using these fields

- Only add documentation comments

- Follow JSDoc @deprecated standard format

SUCCESS CRITERIA:

- All deprecated fields have @deprecated JSDoc comments

- Comments include @since and @remove dates

- Deprecation doc created listing all fields

- Removal timeline reasonable (minimum 6 months)