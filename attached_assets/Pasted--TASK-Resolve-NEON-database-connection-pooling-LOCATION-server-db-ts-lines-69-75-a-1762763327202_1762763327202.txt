**TASK:**  
Resolve NEON database connection pooling.

**LOCATION:**  
server/db.ts (lines 69-75), any other .env, deployment, or CI config containing DATABASE_URL.

**INSTRUCTION:**  
- Update all NEON DATABASE_URL values to use the correct "-pooler" suffix in the hostname (e.g. change to xyz-pooler.us-east-2.aws.neon.tech).
- Programmatically validate pooling at startup: throw error if pooling URL is not present or driver detects serial connection pattern.
- Add test/healthcheck to log connection count and pool size on startup.

**SUCCESS CRITERIA:**  
- No more 'pooling not enabled' warnings in logs.
- App can handle 300+ concurrent requests without database connection errors.
---
**TASK:**  
Refactor product detail fetching logic to eliminate N+1 queries in product-repository.ts (lines 429-656).

**INSTRUCTION:**  
- Rewrite .findFirst or main product query to fetch all required relations (media, certifications, accessories) using Drizzle ORM's 'with' option for eager loading.
- For any child arrays (media, variants): batch fetch using a single IN query, not loops.
- Ensure all list endpoints have .limit() and .offset() for pagination.
- Add instrumentation to log the number of queries per product page load before and after fix.

**SUCCESS CRITERIA:**  
- Queries per product page reduced to 2-3 total (down from 8-12).
- Product page load time under 350ms (was ~2900ms).
- Page returns all product relations correctly.
---
**TASK:**  
Improve large media file (image/video/3D model) delivery.

**LOCATION:**  
media-handlers.ts (lines 664-735), any endpoint that handles object storage.

**INSTRUCTION:**  
- Serve media by generating a short-term (5min) signed CDN/Object Storage URL and redirect user, rather than proxying data through Node.js.
- Implement conditional WebP (or next-gen) compression using 'sharp' for images before serving.
- Add HTTP caching headers (Cache-Control: public, max-age=86400) for all media.
- Ensure endpoints for <video> and <model-viewer> support Range requests/streaming.

**SUCCESS CRITERIA:**  
- All media < 1MB transferred to browser; requests served from CDN, not Node.js.
- LCP on image/video assets < 900ms (was 5-12s).
- Bandwidth consumption reduced by 90%+.
---
**TASK:**  
Reconfigure React Query cache and retry strategy for optimal user experience.

**LOCATION:**  
client/queryClient.ts (lines 232-246), all useQuery/useMutation usage in frontend.

**INSTRUCTION:**  
- Set staleTime per data type (30-60s for dynamic, 5-15min for static, 1-3s for live).
- Set cacheTime = 5x staleTime per query.
- Reduce retry to max 2 attempts; ensure failed queries surface quickly in UI.
- Audit for dynamic query keys or unbounded cache growth — implement cache cleanup as needed.

**SUCCESS CRITERIA:**  
- UX error states now appear in <10s, not minutes.
- Max memory used by cache < 120MB per browser tab.
---
**TASK:**  
Optimize batch and static data queries for manufacturing, media, and config lists.

**INSTRUCTION:**  
- Implement a two-tier cache: L1 (in-memory, 3 min), L2 (Replit KV Store, 30 min).
- Refactor batch and list endpoints to use Promise.all or native async batch, not sequential await or for/while loops.
- Benchmark old vs new API times, and log percent improvement.

**SUCCESS CRITERIA:**  
- Largest batch queries < 300ms.
- KV cache hit rates > 80%.
- API CPU profile is flat, not spiky.
---
**TASK:**  
Reduce initial JavaScript bundle size and improve time to interactive.

**INSTRUCTION:**  
- Enable route-based code splitting in Vite (or React.lazy/suspense) so admin pages are not in initial vendor/main chunk.
- Use vite-plugin-visualizer to find and remove large or unused libraries.
- Implement lazy loading and virtualization for media/content-heavy lists.

**SUCCESS CRITERIA:**  
- Initial JavaScript payload ≤ 800KB gzipped.
- First interactive render < 1.5s on 3G network.
---
**TASK:**  
Raise production cache/KV Store limits and tune fallback.

**INSTRUCTION:**  
- Increase cache max size to 250MB+ for prod; adjust eviction policy (LRU recommended).
- Increase Replit KV fallback timeout to 1.5 seconds.
- Add metrics: log cache hit/miss rates, current memory, eviction events.

**SUCCESS CRITERIA:**  
- KV cache hit rate stays above 85% consistently for main data flows.
- App memory stays within resource limit (no OOM).
---
**TASK:**  
Enforce security, validation, and introduce missing monitoring.

**INSTRUCTION:**  
- CORS: Restrict allowed origins to production domain only.
- POST/PATCH admin routes: require Zod (or equivalent) schema validation, reject any extraneous input.
- Add log sanitization to avoid leaking secrets.
- Deploy or enable alerting for: DB connection errors, slow queries (>500ms), memory over 80%.

**SUCCESS CRITERIA:**  
- Only requests from approved domains accepted.
- No missing validation or stray data in admin routes.
- Sensitive data redact in logs.
- Monitoring/alerts trigger daily on major system errors.
