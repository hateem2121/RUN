Please perform a comprehensive diagnostic sweep of all available console logs and telemetry, focusing on the following:

- Detect all warnings, slow database queries, cache misses, mismatches, and suspicious or frequent requests.
- Survey for alerts, anomalies, repeated errors, and any unusual log entries.
- Identify duplicated logic or unnecessary resource consumption, especially concerning memory, CPU load, and runtime cost.
- Highlight inefficient NEON PostgreSQL queries (missing indexes, overly broad selects, or those not using connection pooling).
- Report any caching problems (KV cache, object storage misses, redundant cache lookups).
- Flag configuration issues (process restarts, environment variable mismatches, failed dependencies).
- Review log patterns for error spikes, slow endpoints (especially Express routes, Drizzle ORM queries), authentication problems, or suspicious traffic.

Use ONLY insights from currently implemented features and existing diagnostics. Do NOT suggest or apply new code changes.

Batch findings in a detailed report structured as follows:

1. Summary Table — List all issues, their frequency, and estimated impact (cost, memory, time).
2. Console Log Analysis — Collate detailed logs for each category (warnings, slow queries, cache issues, request spikes).
3. Resource Efficiency Review — Identify prime candidates for deduplication or optimization; point to exactly where existing logic can be reused or calls can be batched.
4. NEON PostgreSQL-specific Observations — List queries/transactions needing attention for cost and speed improvement (prefer use of connection pooling, parallelized queries, and batch selects).
5. Cache and KV Diagnostics — Show real cache miss rates, duplicated fetches, and any mismatches between app logic and stored keys.
6. Recommendations — Without prescribing implementation steps, enumerate opportunities for removing duplicity and boosting performance given user goals and recent system constraints.

Aim: Provide a structured, actionable diagnostic report on findings for the current stack, focused on performance and efficiency under real usage, without making or proposing code changes.
