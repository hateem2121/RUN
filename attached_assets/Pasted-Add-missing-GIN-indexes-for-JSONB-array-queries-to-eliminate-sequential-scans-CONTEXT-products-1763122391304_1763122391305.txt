Add missing GIN indexes for JSONB array queries to eliminate sequential scans:

CONTEXT:

- products.tags uses @> operator without GIN index (sequential scans on tag searches)

- products.certificate_ids, accessory_ids, image_ids use @> operator in cleanup queries

- Current impact: Tag filtering performs sequential scans, degrades with table size

TASK:

1. Locate the index definition section in shared/schema.ts

2. Add these GIN indexes to the products table index array:

   - products_tags_gin_idx on tags column

   - products_certificate_ids_gin_idx on certificate_ids column

   - products_accessory_ids_gin_idx on accessory_ids column

   - products_image_ids_gin_idx on image_ids column

3. Create migration file in migrations/optimizations/ directory:

   - Name: 002_add_jsonb_gin_indexes.sql

   - Use CREATE INDEX CONCURRENTLY (critical - prevents table locks)

   - Include IF NOT EXISTS checks

   - Add rollback commands in comments

4. Verify the migration syntax matches existing pattern from 001_add_search_indexes.sql

CONSTRAINTS:

- Do NOT add indexes to other tables yet

- Do NOT modify existing query code

- MUST use CONCURRENTLY to avoid production locks

- Follow existing migration file naming convention

SUCCESS CRITERIA:

- Migration file created with 4 GIN indexes

- All use CONCURRENTLY keyword

- Syntax matches existing optimization migration pattern

- Includes rollback commands in comments

---

Add trigram indexes to eliminate sequential scans in ILIKE queries:

CONTEXT:

- getMediaAssets() uses ILIKE on filename, originalName, altText (sequential scans)

- searchProducts() uses LIKE on name, description, sku (sequential scans)

- Current indexes are B-tree (cannot optimize %pattern% searches)

TASK:

1. Check if pg_trgm extension is enabled by searching for CREATE EXTENSION in migrations

2. If not found, create migrations/optimizations/003_enable_trigram_extension.sql:

   - Add: CREATE EXTENSION IF NOT EXISTS pg_trgm;

3. Create migrations/optimizations/004_add_trigram_indexes.sql:

   - Media assets: filename, original_name, alt_text (3 indexes)

   - Products: name, description (2 indexes - skip sku, already has B-tree)

   - Use pattern: CREATE INDEX CONCURRENTLY ... USING GIN (...column gin_trgm_ops)

   

4. Update shared/schema.ts to document these indexes (add comments only, indexes created via SQL)

CONSTRAINTS:

- Do NOT modify repository query code

- MUST use CONCURRENTLY

- Check existing indexes first to avoid duplicates

- Extension must be created before indexes

SUCCESS CRITERIA:

- Extension enabled in migration

- 5 trigram indexes created with CONCURRENTLY

- All use gin_trgm_ops operator class

- No duplicate index names

---

Update existing index creation scripts to use CONCURRENTLY:

CONTEXT:

- File drizzle/optimizations/add_missing_foreign_key_indexes.sql creates indexes WITHOUT CONCURRENTLY

- products_primary_image_id_idx and products_primary_video_id_idx could lock table during creation

- Risk: 50-200ms table locks in production

TASK:

1. Open drizzle/optimizations/add_missing_foreign_key_indexes.sql

2. Find all CREATE INDEX statements that lack CONCURRENTLY keyword

3. Add CONCURRENTLY after INDEX keyword (before IF NOT EXISTS)

4. Verify syntax: CREATE INDEX CONCURRENTLY IF NOT EXISTS ...

5. Document the change:

   - Add comment at top: "-- UPDATED: All indexes now use CONCURRENTLY to prevent table locks"

   - Add timestamp and reason

CONSTRAINTS:

- Do NOT change index names or columns

- Only add CONCURRENTLY keyword

- Do NOT create new migrations

- Preserve all existing IF NOT EXISTS checks

SUCCESS CRITERIA:

- All CREATE INDEX statements in the file use CONCURRENTLY

- Syntax validation passes

- Comment added explaining the change

- No index definitions removed or modified