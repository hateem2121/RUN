**Expert, cost-efficient prompt for Replit Agent 3 with Neon PostgreSQL and Drizzle ORM, November 2025 best practices.**

---

## PRINCIPLES
✅ Use NEON connection from `src/db/index.ts`  
✅ Drizzle ORM for queries—batch and select only required columns  
✅ Do NOT add dependencies  
✅ Refactor only one feature per prompt  
✅ Minimize NEON active time (use KV cache, avoid redundant queries, batch when possible)  
✅ Implement clear logging for all slow/broken routes  
✅ Validate each change in isolation (verify tests pass, no regressions)  
✅ Follow Replit cost control guidelines—prefer Plan Mode, step-by-step approvals

---

## PRIORITY 1: FIX FRONTEND URL GENERATION (P0-1)
- Patch `client/src/components/products/RelatedProducts.tsx` and `ProductGrid.tsx`:
  - Eliminate fallback to `'uncategorized'` or numeric product IDs in URLs.
  - Ensure category slug and product slug are always loaded before URL generation.
  - If either is missing, prevent route navigation and show error.
  - Sync routes with actual database `url_path` values—query using Drizzle ORM and validate slug presence before constructing the link.

#### Verification:
1. Add log statement for every `/api/products/by-path` call, printing both the requested path and actual DB match.
2. Confirm via network and server logs that all product page requests match real DB paths.
3. Validate: 0% 404 rate for valid products after fix.

---

## PRIORITY 2: INCREASE CACHE TTL & OPTIMIZE KV CACHE (P1-3, P2-4)
- For all static endpoints (`/api/homepage-process-cards`, `/api/footer`, `/api/about-hero` etc):
  - in `misc-repository.ts` or relevant repo, update KV cache TTL from 5 min to 30 min.
  - Batch query small tables (homepage batches, content APIs) to minimize NEON round-trips.
  - For large JSONB columns: select only required fields. Avoid `SELECT *` on complex objects—query only fields used by rendering components.

#### Verification:
1. Run before/after tests for each endpoint; logs should show ~30% fewer NEON cold starts, ~60% fewer cache misses.
2. Confirm payload sizes drop for API responses, especially process cards and manufacturing endpoints.

---

## PRIORITY 3: OPTIMIZE HOMEPAGE BATCH (P1-3)
- Refactor homepage batch controller:
  - Use Drizzle ORM `select` to fetch only minimal columns per subquery.
  - For process cards, split large JSONB property into separate endpoint if not needed on homepage load.
  - Enable progressive loading: serve fast sections first; lazy-load slowest (process cards) async.

#### Verification:
1. Batched homepage response <700ms in all cases, as measured by logs.
2. Validate batch endpoint is never blocked by process cards.

---

## PRIORITY 4: LOGGING AND INDEX AUDIT (GENERAL)
- Add detailed index usage logs for all queries >400ms (NEON overhead excluded).
- Create SQL audit routines for all critical tables (`products`, `fabrics`, `certificates`, etc):
  - Ensure all `WHERE` predicates have composite btree indexes.
  - If table is <100 rows, document sequential scan rationale in repo comments.

#### Verification:
1. Commit logs showing index scans for all paths.
2. No missing indexes in Drizzle schema definitions.
3. Document reasoning for sequential scan (small tables).

---

## ARCHITECTURE NOTE (NEON LIMITS)
- Serverless NEON HTTP overhead cannot be directly optimized at code/query level. All DB code must minimize cold requests, maximize cache hits, and avoid redundant calls.
- Document any bottlenecks due to NEON in comment blocks for each repo method affected.

---

## COST OPTIMIZATION
- After each feature fix, test with Plan Mode and lower autonomy for minimal token/data usage.
- Use KV store aggressively for non-critical/below-fold data.
- Regularly prune NEON historical data if point-in-time restore is not required (per NEON docs).

---

## SUCCESS CRITERIA
- 0% 404 rate for valid products
- 70%+ cache hit rate
- Batch endpoints ≤700ms latency
- NEON cold start frequency reduced 80%
- No unnecessary data transfer (minimal payloads)

---

Reference all implementation steps with up-to-date Replit/NEON/Drizzle docs (2025), and run CI/CD tests after every change.