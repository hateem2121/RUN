# Replit AI Agent: Investigation Prompts by Priority

## PRIORITY P0: CRITICAL ISSUES

### 🟥 1. Phantom Product Path Queries (High user and infra impact)
- Extract all log entries related to `[ERROR] 🚨 SLOW QUERY ALERT: getProductByPath exceeded 400ms threshold` and `[ERROR] GET /products/by-path 404`.
- Correlate with frontend navigation events—identify all unique URL paths resulting in 404 and document their sources (category links, product cards, breadcrumbs, etc.).
- Analyze frequency, duration, and user-facing effects of these errors.
- Summarize evidence for frontend routing/URL construction bugs (especially in `enhanced-product-detail.tsx`). Do NOT attempt to fix—report only.
- Recommend investigating prefetch/race conditions in the frontend initiating these calls.

### 🟥 2. Unindexed Static Resource Endpoints (User-facing delay)
- Identify all slow queries (>400ms) on `/api/fabrics` and `/api/certificates` endpoints.
- Validate index usage, explain plan (sequential scan vs. index), and cache hit/miss status.
- Correlate slow requests duration with cache expiration events.
- Highlight NEON HTTP baseline and cold start penalties as dominating factors.
- Summarize findings: confirm if index improvement would be meaningful (given table size).
- Recommend: No implementation, only report on existing state and if composite indexes are missing.

---

## PRIORITY P1: MODERATE

### 🟧 3. Homepage Batch Inefficiency
- Collect all `[WARN] [Homepage Batch] ⚠️ Slow response` log entries.
- Break down batch endpoint latency into constituent queries—quantify database vs. cache vs. deserialization overhead.
- Isolate the specific slow-est sub-queries ("process cards," etc.).
- Assess if Promise.all achieves parallel efficiency or if latency multiples.
- Make no code change recommendations; just document contributions to aggregate latency.

---

## PRIORITY P2: LOW/ACCEPTABLE

### 🟨 4. Cache Deserialization Overhead
- Analyze all cache hit API responses still exceeding 500ms (e.g., `/api/footer`, `/api/about-hero`, etc.).
- Document key/value sizes, JSONB object complexity, and response times.
- Attribute time to Replit KV retrieval, JSON.parse, and network.
- State clearly: these latencies are expected and within tolerance per current infrastructure.

---

## RESOLVED/HISTORICAL: Information Only

### 🟩 5. Cache Warming Failures (Startup Only)
- List and chronologically order all `[ERROR] [Cache Warming]` events from startup sessions.
- Document absence of stack traces and subsequent auto-recovery within the same session.
- Confirm resolution in latest logs; report as transient/no further action.

---

*Do not implement any fixes or changes. Provide evidence-based, root-cause-oriented reports per priority, with log and metric references for each.*
