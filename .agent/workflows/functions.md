---
description: Alias for /neon-functions — Long-running serverless Node.js HTTP functions on Neon.
---

# /functions

**Description:** Alias for `/neon-functions`. Long-running, serverless Node.js HTTP functions deployed onto your Neon branch.

**Usage:** `/functions [deploy | logs | dev]`

## Agent Instructions

When the user invokes `/functions`, execute the `/neon-functions` workflow defined in `.agent/workflows/neon-functions.md` and consult `.agent/skills/neon-functions/SKILL.md`.
