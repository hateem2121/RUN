---
description: Alias for /neon-ai-gateway — Unified LLM proxy and multi-model routing on Neon.
---

# /ai-gateway

**Description:** Alias for `/neon-ai-gateway`. One API and one credential for frontier and open-source LLMs built into your Neon branch.

**Usage:** `/ai-gateway [model | config | prompt]`

## Agent Instructions

When the user invokes `/ai-gateway`, execute the `/neon-ai-gateway` workflow defined in `.agent/workflows/neon-ai-gateway.md` and consult `.agent/skills/neon-ai-gateway/SKILL.md`.
