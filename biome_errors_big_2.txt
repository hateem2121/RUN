.config/.semgrep/semgrep_rules.json check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i The size of the file is 1.7 MiB, which exceeds the configured maximum of 1.0 MiB for this project.
    Use the `files.maxSize` configuration to change the maximum size of files processed, or `files.includes` to ignore the file.
  

.config/replit/.semgrep/semgrep_rules.json check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i The size of the file is 1.7 MiB, which exceeds the configured maximum of 1.0 MiB for this project.
    Use the `files.maxSize` configuration to change the maximum size of files processed, or `files.includes` to ignore the file.
  

clean_lint.json check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i The size of the file is 5.4 MiB, which exceeds the configured maximum of 1.0 MiB for this project.
    Use the `files.maxSize` configuration to change the maximum size of files processed, or `files.includes` to ignore the file.
  

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:403:14 lint/complexity/useLiteralKeys  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━

  i The computed expression can be simplified without the use of a string literal.
  
    401 │     // Additional validations
    402 │     if (data.imageIds.length === 0 && !data.primaryImageId) {
  > 403 │       errors["images"] = "At least one product image is required";
        │              ^^^^^^^^
    404 │       errorCount++;
    405 │     }
  
  i Unsafe fix: Use a literal key instead.
  
     401  401 │       // Additional validations
     402  402 │       if (data.imageIds.length === 0 && !data.primaryImageId) {
     403      │ - ······errors["images"]·=·"At·least·one·product·image·is·required";
          403 │ + ······errors.images·=·"At·least·one·product·image·is·required";
     404  404 │         errorCount++;
     405  405 │       }
  

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:408:14 lint/complexity/useLiteralKeys  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━

  i The computed expression can be simplified without the use of a string literal.
  
    407 │     if (data.certificateIds.length === 0) {
  > 408 │       errors["certificates"] = "Consider adding sustainability certificates for B2B credibility";
        │              ^^^^^^^^^^^^^^
    409 │       warningCount++;
    410 │     }
  
  i Unsafe fix: Use a literal key instead.
  
     406  406 │   
     407  407 │       if (data.certificateIds.length === 0) {
     408      │ - ······errors["certificates"]·=·"Consider·adding·sustainability·certificates·for·B2B·credibility";
          408 │ + ······errors.certificates·=·"Consider·adding·sustainability·certificates·for·B2B·credibility";
     409  409 │         warningCount++;
     410  410 │       }
  

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:413:14 lint/complexity/useLiteralKeys  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━

  i The computed expression can be simplified without the use of a string literal.
  
    412 │     if (Object.keys(data.technicalSpecs || {}).length === 0) {
  > 413 │       errors["technicalSpecs"] =
        │              ^^^^^^^^^^^^^^^^
    414 │         "Technical specifications help B2B customers make informed decisions";
    415 │       warningCount++;
  
  i Unsafe fix: Use a literal key instead.
  
     411  411 │   
     412  412 │       if (Object.keys(data.technicalSpecs || {}).length === 0) {
     413      │ - ······errors["technicalSpecs"]·=
          413 │ + ······errors.technicalSpecs·=
     414  414 │           "Technical specifications help B2B customers make informed decisions";
     415  415 │         warningCount++;
  

client/src/components/admin/unified-sustainability-management.tsx:153:7 lint/complexity/useLiteralKeys  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  i The computed expression can be simplified without the use of a string literal.
  
    151 │       Target,
    152 │       TrendingUp,
  > 153 │     }["Leaf"] || Leaf;
        │       ^^^^^^
    154 │ 
    155 │   return (
  
  i Unsafe fix: Use a literal key instead.
  
    151 151 │         Target,
    152 152 │         TrendingUp,
    153     │ - ····}["Leaf"]·||·Leaf;
        153 │ + ····}.Leaf·||·Leaf;
    154 154 │   
    155 155 │     return (
  

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on distortAmount.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    464 │         uSpotlightOpacity: { value: spotlightOpacity },
    465 │         uMirror: { value: mirrorGradient ? 1 : 0 },
  > 466 │         uDistort: { value: distortAmount },
        │                            ^^^^^^^^^^^^^
    467 │         uShineFlip: { value: shineDirection === "right" ? 1 : 0 },
    468 │         uColor0: { value: colorArr[0]! },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·distortAmount]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               +++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on callIfFn.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    578 │         callIfFn(geometryRef.current, "remove");
    579 │         callIfFn(meshRef.current as unknown as { remove?: () => void }, "remove");
  > 580 │         callIfFn(rendererRef.current as unknown as { destroy?: () => void }, "destroy");
        │         ^^^^^^^^
    581 │         programRef.current = null;
    582 │         geometryRef.current = null;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    577 │         callIfFn(programRef.current, "remove");
    578 │         callIfFn(geometryRef.current, "remove");
  > 579 │         callIfFn(meshRef.current as unknown as { remove?: () => void }, "remove");
        │         ^^^^^^^^
    580 │         callIfFn(rendererRef.current as unknown as { destroy?: () => void }, "destroy");
    581 │         programRef.current = null;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    576 │         // Advanced cleanup pattern from specification
    577 │         callIfFn(programRef.current, "remove");
  > 578 │         callIfFn(geometryRef.current, "remove");
        │         ^^^^^^^^
    579 │         callIfFn(meshRef.current as unknown as { remove?: () => void }, "remove");
    580 │         callIfFn(rendererRef.current as unknown as { destroy?: () => void }, "destroy");
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    576 │         // Advanced cleanup pattern from specification
  > 577 │         callIfFn(programRef.current, "remove");
        │         ^^^^^^^^
    578 │         callIfFn(geometryRef.current, "remove");
    579 │         callIfFn(meshRef.current as unknown as { remove?: () => void }, "remove");
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·callIfFn]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++                                                                

client/src/components/GradientBlinds.tsx:193:36 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    192 │   // Ensure at least 2 colors for gradient
  > 193 │   if (base.length === 1) base.push(base[0]!);
        │                                    ^^^^^^^^
    194 │ 
    195 │   // Pad array to MAX_COLORS for consistent shader uniforms
  

client/src/components/GradientBlinds.tsx:196:46 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    195 │   // Pad array to MAX_COLORS for consistent shader uniforms
  > 196 │   while (base.length < MAX_COLORS) base.push(base[base.length - 1]!);
        │                                              ^^^^^^^^^^^^^^^^^^^^^^
    197 │ 
    198 │   // Convert to RGB arrays for WebGL
  

client/src/components/GradientBlinds.tsx:200:58 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    198 │   // Convert to RGB arrays for WebGL
    199 │   const arr: [number, number, number][] = [];
  > 200 │   for (let i = 0; i < MAX_COLORS; i++) arr.push(hexToRGB(base[i]!));
        │                                                          ^^^^^^^^
    201 │ 
    202 │   const count = Math.max(2, Math.min(MAX_COLORS, stops.length));
  

client/src/components/GradientBlinds.tsx:468:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    466 │         uDistort: { value: distortAmount },
    467 │         uShineFlip: { value: shineDirection === "right" ? 1 : 0 },
  > 468 │         uColor0: { value: colorArr[0]! },
        │                           ^^^^^^^^^^^^
    469 │         uColor1: { value: colorArr[1]! },
    470 │         uColor2: { value: colorArr[2]! },
  

client/src/components/GradientBlinds.tsx:469:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    467 │         uShineFlip: { value: shineDirection === "right" ? 1 : 0 },
    468 │         uColor0: { value: colorArr[0]! },
  > 469 │         uColor1: { value: colorArr[1]! },
        │                           ^^^^^^^^^^^^
    470 │         uColor2: { value: colorArr[2]! },
    471 │         uColor3: { value: colorArr[3]! },
  

client/src/components/GradientBlinds.tsx:470:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    468 │         uColor0: { value: colorArr[0]! },
    469 │         uColor1: { value: colorArr[1]! },
  > 470 │         uColor2: { value: colorArr[2]! },
        │                           ^^^^^^^^^^^^
    471 │         uColor3: { value: colorArr[3]! },
    472 │         uColor4: { value: colorArr[4]! },
  

client/src/components/GradientBlinds.tsx:471:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    469 │         uColor1: { value: colorArr[1]! },
    470 │         uColor2: { value: colorArr[2]! },
  > 471 │         uColor3: { value: colorArr[3]! },
        │                           ^^^^^^^^^^^^
    472 │         uColor4: { value: colorArr[4]! },
    473 │         uColor5: { value: colorArr[5]! },
  

client/src/components/GradientBlinds.tsx:472:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    470 │         uColor2: { value: colorArr[2]! },
    471 │         uColor3: { value: colorArr[3]! },
  > 472 │         uColor4: { value: colorArr[4]! },
        │                           ^^^^^^^^^^^^
    473 │         uColor5: { value: colorArr[5]! },
    474 │         uColor6: { value: colorArr[6]! },
  

client/src/components/GradientBlinds.tsx:473:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    471 │         uColor3: { value: colorArr[3]! },
    472 │         uColor4: { value: colorArr[4]! },
  > 473 │         uColor5: { value: colorArr[5]! },
        │                           ^^^^^^^^^^^^
    474 │         uColor6: { value: colorArr[6]! },
    475 │         uColor7: { value: colorArr[7]! },
  

client/src/components/GradientBlinds.tsx:474:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    472 │         uColor4: { value: colorArr[4]! },
    473 │         uColor5: { value: colorArr[5]! },
  > 474 │         uColor6: { value: colorArr[6]! },
        │                           ^^^^^^^^^^^^
    475 │         uColor7: { value: colorArr[7]! },
    476 │         uColorCount: { value: colorCount },
  

client/src/components/GradientBlinds.tsx:605:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    603 │       const { arr: colorStops, count: colorCount } = prepStops(gradientColors);
    604 │       for (let i = 0; i < MAX_COLORS; i++) {
  > 605 │         const [r, g, b] = colorStops[i]!;
        │                           ^^^^^^^^^^^^^^
    606 │         if (program.uniforms[`uColor${i}`]) {
    607 │           program.uniforms[`uColor${i}`].value = [r, g, b];
  

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on angle.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    457 │         iMouse: { value: [0, 0] },
    458 │         iTime: { value: 0 },
  > 459 │         uAngle: { value: (angle * Math.PI) / 180 },
        │                           ^^^^^
    460 │         uNoise: { value: noise },
    461 │         uBlindCount: { value: Math.max(1, blindCount) },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·angle]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               +++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on blindMinWidth.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    497 │         uniforms.iResolution.value = [gl.drawingBufferWidth, gl.drawingBufferHeight, 1];
    498 │ 
  > 499 │         if (blindMinWidth && blindMinWidth > 0) {
        │                              ^^^^^^^^^^^^^
    500 │           const maxByMinWidth = Math.max(1, Math.floor(rect.width / blindMinWidth));
    501 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    497 │         uniforms.iResolution.value = [gl.drawingBufferWidth, gl.drawingBufferHeight, 1];
    498 │ 
  > 499 │         if (blindMinWidth && blindMinWidth > 0) {
        │             ^^^^^^^^^^^^^
    500 │           const maxByMinWidth = Math.max(1, Math.floor(rect.width / blindMinWidth));
    501 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    499 │         if (blindMinWidth && blindMinWidth > 0) {
  > 500 │           const maxByMinWidth = Math.max(1, Math.floor(rect.width / blindMinWidth));
        │                                                                     ^^^^^^^^^^^^^
    501 │ 
    502 │           const effective = blindCount ? Math.min(blindCount, maxByMinWidth) : maxByMinWidth;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·blindMinWidth]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               +++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on mirrorGradient.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    463 │         uSpotlightSoftness: { value: spotlightSoftness },
    464 │         uSpotlightOpacity: { value: spotlightOpacity },
  > 465 │         uMirror: { value: mirrorGradient ? 1 : 0 },
        │                           ^^^^^^^^^^^^^^
    466 │         uDistort: { value: distortAmount },
    467 │         uShineFlip: { value: shineDirection === "right" ? 1 : 0 },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·mirrorGradient]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on spotlightSoftness.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    461 │         uBlindCount: { value: Math.max(1, blindCount) },
    462 │         uSpotlightRadius: { value: spotlightRadius },
  > 463 │         uSpotlightSoftness: { value: spotlightSoftness },
        │                                      ^^^^^^^^^^^^^^^^^
    464 │         uSpotlightOpacity: { value: spotlightOpacity },
    465 │         uMirror: { value: mirrorGradient ? 1 : 0 },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·spotlightSoftness]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               +++++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on webglInitialized.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    552 │             renderer.render({ scene: meshRef.current });
    553 │             // Notify parent when WebGL is ready after first successful render
  > 554 │             if (!webglInitialized && t > 100) {
        │                  ^^^^^^^^^^^^^^^^
    555 │               // Small delay to ensure stability
    556 │               setWebglInitialized(true);
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·webglInitialized]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on spotlightRadius.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    460 │         uNoise: { value: noise },
    461 │         uBlindCount: { value: Math.max(1, blindCount) },
  > 462 │         uSpotlightRadius: { value: spotlightRadius },
        │                                    ^^^^^^^^^^^^^^^
    463 │         uSpotlightSoftness: { value: spotlightSoftness },
    464 │         uSpotlightOpacity: { value: spotlightOpacity },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·spotlightRadius]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               +++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on spotlightOpacity.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    462 │         uSpotlightRadius: { value: spotlightRadius },
    463 │         uSpotlightSoftness: { value: spotlightSoftness },
  > 464 │         uSpotlightOpacity: { value: spotlightOpacity },
        │                                     ^^^^^^^^^^^^^^^^
    465 │         uMirror: { value: mirrorGradient ? 1 : 0 },
    466 │         uDistort: { value: distortAmount },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·spotlightOpacity]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on shineDirection.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    465 │         uMirror: { value: mirrorGradient ? 1 : 0 },
    466 │         uDistort: { value: distortAmount },
  > 467 │         uShineFlip: { value: shineDirection === "right" ? 1 : 0 },
        │                              ^^^^^^^^^^^^^^
    468 │         uColor0: { value: colorArr[0]! },
    469 │         uColor1: { value: colorArr[1]! },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·shineDirection]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on paused.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    548 │           lastTimeRef.current = t;
    549 │         }
  > 550 │         if (!paused && programRef.current && meshRef.current) {
        │              ^^^^^^
    551 │           try {
    552 │             renderer.render({ scene: meshRef.current });
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·paused]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on noise.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    458 │         iTime: { value: 0 },
    459 │         uAngle: { value: (angle * Math.PI) / 180 },
  > 460 │         uNoise: { value: noise },
        │                          ^^^^^
    461 │         uBlindCount: { value: Math.max(1, blindCount) },
    462 │         uSpotlightRadius: { value: spotlightRadius },
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·noise]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               +++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on gradientColors.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    491 │       console.log("[GradientBlinds] WebGL renderer initialized successfully");
  > 492 │       console.log("[GradientBlinds] Gradient colors:", gradientColors);
        │                                                        ^^^^^^^^^^^^^^
    493 │ 
    494 │       const resize = () => {
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    428 │ `;
    429 │ 
  > 430 │       const { arr: colorArr, count: colorCount } = prepStops(gradientColors);
        │                                                              ^^^^^^^^^^^^^^
    431 │       const uniforms: {
    432 │         iResolution: { value: [number, number, number] };
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·gradientColors]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on mouseDampening.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    534 │         rafRef.current = requestAnimationFrame(loop);
    535 │         uniforms.iTime.value = t * 0.001;
  > 536 │         if (mouseDampening > 0) {
        │             ^^^^^^^^^^^^^^
    537 │           if (!lastTimeRef.current) lastTimeRef.current = t;
    538 │           const dt = (t - lastTimeRef.current) / 1000;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    538 │           const dt = (t - lastTimeRef.current) / 1000;
    539 │           lastTimeRef.current = t;
  > 540 │           const tau = Math.max(1e-4, mouseDampening);
        │                                      ^^^^^^^^^^^^^^
    541 │           let factor = 1 - Math.exp(-dt / tau);
    542 │           if (factor > 1) factor = 1;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    525 │         const y = (rect.height - (e.clientY - rect.top)) * scale;
    526 │         mouseTargetRef.current = [x, y];
  > 527 │         if (mouseDampening <= 0) {
        │             ^^^^^^^^^^^^^^
    528 │           uniforms.iMouse.value = [x, y];
    529 │         }
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·mouseDampening]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++++++                                                                

client/src/components/GradientBlinds.tsx:283:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on blindCount.
  
    281 │   // OPTIMIZED: Separate WebGL context creation from uniform updates
    282 │   // This prevents full rebuilds on every prop change
  > 283 │   useEffect(() => {
        │   ^^^^^^^^^
    284 │     if (!webglSupport?.supported) return undefined;
    285 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    459 │         uAngle: { value: (angle * Math.PI) / 180 },
    460 │         uNoise: { value: noise },
  > 461 │         uBlindCount: { value: Math.max(1, blindCount) },
        │                                           ^^^^^^^^^^
    462 │         uSpotlightRadius: { value: spotlightRadius },
    463 │         uSpotlightSoftness: { value: spotlightSoftness },
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    503 │           uniforms.uBlindCount.value = Math.max(1, effective);
    504 │         } else {
  > 505 │           uniforms.uBlindCount.value = Math.max(1, blindCount);
        │                                                    ^^^^^^^^^^
    506 │         }
    507 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    500 │           const maxByMinWidth = Math.max(1, Math.floor(rect.width / blindMinWidth));
    501 │ 
  > 502 │           const effective = blindCount ? Math.min(blindCount, maxByMinWidth) : maxByMinWidth;
        │                                                   ^^^^^^^^^^
    503 │           uniforms.uBlindCount.value = Math.max(1, effective);
    504 │         } else {
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    500 │           const maxByMinWidth = Math.max(1, Math.floor(rect.width / blindMinWidth));
    501 │ 
  > 502 │           const effective = blindCount ? Math.min(blindCount, maxByMinWidth) : maxByMinWidth;
        │                             ^^^^^^^^^^
    503 │           uniforms.uBlindCount.value = Math.max(1, effective);
    504 │         } else {
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    593 │ ··},·[webglSupport?.supported,·dpr,·onWebGLReady,·onWebGLError,·blindCount]);·//·OPTIMIZED:·Only·rebuild·context·when·absolutely·necessary
        │                                                               ++++++++++++                                                                

client/src/components/GradientBlinds.tsx:475:27 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    473 │         uColor5: { value: colorArr[5]! },
    474 │         uColor6: { value: colorArr[6]! },
  > 475 │         uColor7: { value: colorArr[7]! },
        │                           ^^^^^^^^^^^^
    476 │         uColorCount: { value: colorCount },
    477 │       };
  

client/src/components/LightRays.tsx:36:44 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    34 │   const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
    35 │   return m
  > 36 │     ? [parseInt(m[1]!, 16) / 255, parseInt(m[2]!, 16) / 255, parseInt(m[3]!, 16) / 255]
       │                                            ^^^^^
    37 │     : [1, 1, 1];
    38 │ };
  

client/src/components/LightRays.tsx:36:71 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    34 │   const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
    35 │   return m
  > 36 │     ? [parseInt(m[1]!, 16) / 255, parseInt(m[2]!, 16) / 255, parseInt(m[3]!, 16) / 255]
       │                                                                       ^^^^^
    37 │     : [1, 1, 1];
    38 │ };
  

client/src/components/LightRays.tsx:36:17 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    34 │   const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
    35 │   return m
  > 36 │     ? [parseInt(m[1]!, 16) / 255, parseInt(m[2]!, 16) / 255, parseInt(m[3]!, 16) / 255]
       │                 ^^^^^
    37 │     : [1, 1, 1];
    38 │ };
  

client/src/components/admin/CertificationsTabContent.tsx:46:73 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    44 │                     key={certificate.id}
    45 │                     className={`flex items-center space-x-3 p-3 rounded-lg border cursor-pointer transition-colors ${
  > 46 │                       (localForm.data?.certificationIds || []).includes(certificate.id!)
       │                                                                         ^^^^^^^^^^^^^^^
    47 │                         ? "bg-green-50 border-green-200"
    48 │                         : "bg-gray-50 border-gray-200 hover:bg-gray-100"
  

client/src/components/admin/CertificationsTabContent.tsx:52:58 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    50 │                     onClick={() => {
    51 │                       const currentIds = localForm.data?.certificationIds || [];
  > 52 │                       const newIds = currentIds.includes(certificate.id!)
       │                                                          ^^^^^^^^^^^^^^^
    53 │                         ? currentIds.filter((id: number) => id !== certificate.id!)
    54 │                         : [...currentIds, certificate.id!];
  

client/src/components/admin/CertificationsTabContent.tsx:53:68 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    51 │                       const currentIds = localForm.data?.certificationIds || [];
    52 │                       const newIds = currentIds.includes(certificate.id!)
  > 53 │                         ? currentIds.filter((id: number) => id !== certificate.id!)
       │                                                                    ^^^^^^^^^^^^^^^
    54 │                         : [...currentIds, certificate.id!];
    55 │                       onLocalUpdate({
  

client/src/components/admin/CertificationsTabContent.tsx:54:43 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    52 │                       const newIds = currentIds.includes(certificate.id!)
    53 │                         ? currentIds.filter((id: number) => id !== certificate.id!)
  > 54 │                         : [...currentIds, certificate.id!];
       │                                           ^^^^^^^^^^^^^^^
    55 │                       onLocalUpdate({
    56 │                         data: {
  

client/src/components/admin/CertificationsTabContent.tsx:65:75 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    63 │                     <div
    64 │                       className={`w-4 h-4 rounded border-2 flex items-center justify-center ${
  > 65 │                         (localForm.data?.certificationIds || []).includes(certificate.id!)
       │                                                                           ^^^^^^^^^^^^^^^
    66 │                           ? "bg-green-600 border-green-600"
    67 │                           : "border-gray-300"
  

client/src/components/admin/CertificationsTabContent.tsx:70:74 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    68 │                       }`}
    69 │                     >
  > 70 │                       {(localForm.data?.certificationIds || []).includes(certificate.id!) && (
       │                                                                          ^^^^^^^^^^^^^^^
    71 │                         <svg className="w-3 h-3 text-white" fill="currentColor" viewBox="0 0 20 20">
    72 │                           <path
  

client/src/components/admin/about-sections-tab.tsx:262:30 lint/style/noNonNullAssertion  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    260 │           // Reconcile: preserve order from mediaIds, remove missing IDs
    261 │           const validIds = allMedia.map((m) => m.id);
  > 262 │           const reconciled = section.mediaIds!.filter((id) => validIds.includes(id));
        │                              ^^^^^^^^^^^^^^^^^
    263 │           const orderedMedia = reconciled.map((id) => allMedia.find((m) => m.id === id)!);
    264 │ 
  
  i Unsafe fix: Replace with optional chain operator ?. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator
  
    260 260 │             // Reconcile: preserve order from mediaIds, remove missing IDs
    261 261 │             const validIds = allMedia.map((m) => m.id);
    262     │ - ··········const·reconciled·=·section.mediaIds!.filter((id)·=>·validIds.includes(id));
        262 │ + ··········const·reconciled·=·section.mediaIds?.filter((id)·=>·validIds.includes(id));
    263 263 │             const orderedMedia = reconciled.map((id) => allMedia.find((m) => m.id === id)!);
    264 264 │   
  

client/src/components/admin/about-sections-tab.tsx:263:55 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    261 │           const validIds = allMedia.map((m) => m.id);
    262 │           const reconciled = section.mediaIds!.filter((id) => validIds.includes(id));
  > 263 │           const orderedMedia = reconciled.map((id) => allMedia.find((m) => m.id === id)!);
        │                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    264 │ 
    265 │           setSelectedMedia(orderedMedia);
  

client/src/components/admin/categories/CategoryDisplay.tsx:63:26 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    61 │           const target = e.currentTarget;
    62 │           target.style.display = "none";
  > 63 │           const parent = target.parentElement!;
       │                          ^^^^^^^^^^^^^^^^^^^^^
    64 │           parent.innerHTML =
    65 │             '<div class="w-full h-full flex items-center justify-center text-gray-400"><svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg></div>';
  

client/src/components/admin/categories/CategoryForm.tsx:118:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: mode.
  
    117 │   // Initialize form data
  > 118 │   useEffect(() => {
        │   ^^^^^^^^^
    119 │     if (initialData) {
    120 │       setFormData({
  
  i Outer scope values aren't valid dependencies because mutating them doesn't re-render the component.
  
    157 │       });
    158 │     }
  > 159 │   }, [initialData, mode]);
        │                    ^^^^
    160 │ 
    161 │   // Auto-generate slug from name
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    159 │ ··},·[initialData,·mode]);
        │                  ------   

client/src/components/admin/categories/CategoryForm.tsx:162:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on formData.slug.
  
    161 │   // Auto-generate slug from name
  > 162 │   useEffect(() => {
        │   ^^^^^^^^^
    163 │     if (mode === "create" && formData.name && !formData.slug) {
    164 │       const slug = formData.name
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    161 │   // Auto-generate slug from name
    162 │   useEffect(() => {
  > 163 │     if (mode === "create" && formData.name && !formData.slug) {
        │                                                ^^^^^^^^^^^^^
    164 │       const slug = formData.name
    165 │         .toLowerCase()
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    172 │ ··},·[formData.name,·mode,·formData.slug]);
        │                          +++++++++++++++   

client/src/components/admin/categories/CategoryList.tsx:111:38 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    110 │       categories.forEach((category) => {
  > 111 │         const categoryWithChildren = categoryMap.get(category.id)!;
        │                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    112 │ 
    113 │         if (category.parentId && categoryMap.has(category.parentId)) {
  

client/src/components/admin/categories/CategoryList.tsx:114:26 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    113 │         if (category.parentId && categoryMap.has(category.parentId)) {
  > 114 │           const parent = categoryMap.get(category.parentId)!;
        │                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    115 │           parent.children = parent.children || [];
    116 │           parent.children.push(categoryWithChildren);
  

client/src/components/admin/fabric-management-enhanced-v2.tsx:271:36 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on parseNumericValue.
  
    269 │   });
    270 │ 
  > 271 │   const filteredAndSortedFabrics = useMemo(() => {
        │                                    ^^^^^^^
    272 │     const filtered = fabrics.filter((fabric) => {
    273 │       // Search filter
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    301 │       // Sustainability filter
    302 │       if (filters.sustainability !== "all") {
  > 303 │         const score = parseNumericValue(fabric.sustainabilityScore || "");
        │                       ^^^^^^^^^^^^^^^^^
    304 │         if (filters.sustainability === "highSustainability" && (!score || score < 4)) return false;
    305 │         if (filters.sustainability === "mediumSustainability" && score !== 3) return false;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    291 │       // Weight category filter
    292 │       if (filters.weightCategory !== "all" && fabric.weight) {
  > 293 │         const weight = parseNumericValue(fabric.weight);
        │                        ^^^^^^^^^^^^^^^^^
    294 │         if (weight !== null) {
    295 │           if (filters.weightCategory === "light" && weight >= 150) return false;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    354 │ ··},·[fabrics,·searchTerm,·filters,·sortBy,·sortDirection,·parseNumericValue]);
        │                                                          +++++++++++++++++++   

client/src/components/admin/fabric-management-enhanced-v2.tsx:357:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on parseNumericValue.
  
    356 │   // Analytics calculations
  > 357 │   useMemo(() => {
        │   ^^^^^^^
    358 │     const totalFabrics = fabrics.length;
    359 │     const activeFabrics = fabrics.filter((f) => f.isActive).length;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    383 │     const avgSustainabilityScore = (() => {
    384 │       const validScores = fabrics
  > 385 │         .map((f) => parseNumericValue(f.sustainabilityScore || ""))
        │                     ^^^^^^^^^^^^^^^^^
    386 │         .filter((s) => s !== null) as number[];
    387 │       return validScores.length > 0
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    376 │       }).length,
    377 │       heavy: fabrics.filter((f) => {
  > 378 │         const weight = parseNumericValue(f.weight || "");
        │                        ^^^^^^^^^^^^^^^^^
    379 │         return weight && weight > 300;
    380 │       }).length,
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    372 │       }).length,
    373 │       medium: fabrics.filter((f) => {
  > 374 │         const weight = parseNumericValue(f.weight || "");
        │                        ^^^^^^^^^^^^^^^^^
    375 │         return weight && weight >= 150 && weight <= 300;
    376 │       }).length,
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    368 │     const weightDistribution = {
    369 │       light: fabrics.filter((f) => {
  > 370 │         const weight = parseNumericValue(f.weight || "");
        │                        ^^^^^^^^^^^^^^^^^
    371 │         return weight && weight < 150;
    372 │       }).length,
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    359 │     const activeFabrics = fabrics.filter((f) => f.isActive).length;
    360 │     const sustainableFabrics = fabrics.filter((f) => {
  > 361 │       const score = parseNumericValue(f.sustainabilityScore || "");
        │                     ^^^^^^^^^^^^^^^^^
    362 │       return score && score >= 4;
    363 │     }).length;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    411 │ ··},·[fabrics,·parseNumericValue]);
        │              +++++++++++++++++++   

client/src/components/admin/fiber-management.tsx:990:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on handleKeyDown.
  
    989 │   // Add keyboard event listener
  > 990 │   useEffect(() => {
        │   ^^^^^^^^^
    991 │     document.addEventListener("keydown", handleKeyDown);
    992 │     return () => document.removeEventListener("keydown", handleKeyDown);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    989 │   // Add keyboard event listener
    990 │   useEffect(() => {
  > 991 │     document.addEventListener("keydown", handleKeyDown);
        │                                          ^^^^^^^^^^^^^
    992 │     return () => document.removeEventListener("keydown", handleKeyDown);
    993 │   }, [selectedFibers.size]); // eslint-disable-line react-hooks/exhaustive-deps
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    990 │   useEffect(() => {
    991 │     document.addEventListener("keydown", handleKeyDown);
  > 992 │     return () => document.removeEventListener("keydown", handleKeyDown);
        │                                                          ^^^^^^^^^^^^^
    993 │   }, [selectedFibers.size]); // eslint-disable-line react-hooks/exhaustive-deps
    994 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    993 │ ··},·[selectedFibers.size,·handleKeyDown]);·//·eslint-disable-line·react-hooks/exhaustive-deps
        │                          +++++++++++++++                                                      

client/src/components/admin/fiber-management.tsx:990:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: selectedFibers.size.
  
    989 │   // Add keyboard event listener
  > 990 │   useEffect(() => {
        │   ^^^^^^^^^
    991 │     document.addEventListener("keydown", handleKeyDown);
    992 │     return () => document.removeEventListener("keydown", handleKeyDown);
  
  i This dependency can be removed from the list.
  
    991 │     document.addEventListener("keydown", handleKeyDown);
    992 │     return () => document.removeEventListener("keydown", handleKeyDown);
  > 993 │   }, [selectedFibers.size]); // eslint-disable-line react-hooks/exhaustive-deps
        │       ^^^^^^^^^^^^^^^^^^^
    994 │ 
    995 │   // Filter and sort fibers based on search, filter, and sort criteria
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    993 │ ··},·[selectedFibers.size]);·//·eslint-disable-line·react-hooks/exhaustive-deps
        │       -------------------                                                      

client/src/components/admin/folder-tree.tsx:228:17 lint/style/noNonNullAssertion  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    226 │         </ContextMenu>
    227 │         {isExpanded && hasChildren && (
  > 228 │           <div>{folder.children!.map((child) => renderFolder(child, level + 1))}</div>
        │                 ^^^^^^^^^^^^^^^^
    229 │         )}
    230 │       </div>
  
  i Unsafe fix: Replace with optional chain operator ?. This operator includes runtime checks, so it is safer than the compile-only non-null assertion operator
  
    226 226 │           </ContextMenu>
    227 227 │           {isExpanded && hasChildren && (
    228     │ - ··········<div>{folder.children!.map((child)·=>·renderFolder(child,·level·+·1))}</div>
        228 │ + ··········<div>{folder.children?.map((child)·=>·renderFolder(child,·level·+·1))}</div>
    229 229 │           )}
    230 230 │         </div>
  

client/src/components/admin/media-library/MediaGrid.tsx:597:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on params.get.
  
    596 │   // 🔍 PAGINATION SYNC MONITOR: Track frontend-backend pagination alignment
  > 597 │   useEffect(() => {
        │   ^^^^^^^^^
    598 │     console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
    599 │     console.log("📊 PAGINATION SYNC MONITOR - MediaGrid");
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    608 │     console.log("  - URL:", apiUrl);
    609 │     console.log("  - page param:", params.get("page"), "(sent to backend)");
  > 610 │     console.log("  - limit param:", params.get("limit"), "(sent to backend)");
        │                                     ^^^^^^^^^^
    611 │     console.log("🔹 Cache Key:");
    612 │     console.log("  - page:", state.currentPage);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    607 │     console.log("🔹 API Request:");
    608 │     console.log("  - URL:", apiUrl);
  > 609 │     console.log("  - page param:", params.get("page"), "(sent to backend)");
        │                                    ^^^^^^^^^^
    610 │     console.log("  - limit param:", params.get("limit"), "(sent to backend)");
    611 │     console.log("🔹 Cache Key:");
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    617 │ ··},·[selectionMode,·isStandalone,·state.currentPage,·state.totalPages,·apiUrl,·params.get]);
        │                                                                               ++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:597:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on queryKey.
  
    596 │   // 🔍 PAGINATION SYNC MONITOR: Track frontend-backend pagination alignment
  > 597 │   useEffect(() => {
        │   ^^^^^^^^^
    598 │     console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
    599 │     console.log("📊 PAGINATION SYNC MONITOR - MediaGrid");
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    612 │     console.log("  - page:", state.currentPage);
    613 │     console.log("  - limit:", 24);
  > 614 │     console.log("  - queryKey:", queryKey);
        │                                  ^^^^^^^^
    615 │     console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
    616 │     // eslint-disable-next-line react-hooks/exhaustive-deps
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    617 │ ··},·[selectionMode,·isStandalone,·state.currentPage,·state.totalPages,·apiUrl,·queryKey]);
        │                                                                               ++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:838:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on pagination.limit.
  
    837 │   // 🔍 API RESPONSE VALIDATION: Verify backend response matches request
  > 838 │   useEffect(() => {
        │   ^^^^^^^^^
    839 │     if (mediaResponse && status === "success") {
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    866 │         "  - Request limit = Response limit?",
    867 │         limitMatch ? "✓" : "✗",
  > 868 │         `(${requestLimit} === ${pagination.limit})`,
        │                                 ^^^^^^^^^^^^^^^^
    869 │       );
    870 │       console.log(
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    853 │       console.log("📥 Response:");
    854 │       console.log("  - backend page:", pagination.page);
  > 855 │       console.log("  - backend limit:", pagination.limit);
        │                                         ^^^^^^^^^^^^^^^^
    856 │       console.log("  - backend totalPages:", pagination.totalPages);
    857 │       console.log("  - backend total items:", pagination.total);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    841 │       const requestLimit = Number.parseInt(params.get("limit") || "24", 10);
    842 │       const pageMatch = requestPage === pagination.page;
  > 843 │       const limitMatch = requestLimit === pagination.limit;
        │                                           ^^^^^^^^^^^^^^^^
    844 │       const frontendMatch = state.currentPage === pagination.page;
    845 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    881 │ ··},·[mediaResponse,·status,·state.currentPage,·displayAssets.length,·pagination.limit]);
        │                                                                     ++++++++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:838:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on pagination.total.
  
    837 │   // 🔍 API RESPONSE VALIDATION: Verify backend response matches request
  > 838 │   useEffect(() => {
        │   ^^^^^^^^^
    839 │     if (mediaResponse && status === "success") {
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    855 │       console.log("  - backend limit:", pagination.limit);
    856 │       console.log("  - backend totalPages:", pagination.totalPages);
  > 857 │       console.log("  - backend total items:", pagination.total);
        │                                               ^^^^^^^^^^^^^^^^
    858 │       console.log("  - items received:", displayAssets.length);
    859 │       console.log("🔍 Consistency Check:");
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    881 │ ··},·[mediaResponse,·status,·state.currentPage,·displayAssets.length,·pagination.total]);
        │                                                                     ++++++++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:838:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on params.get.
  
    837 │   // 🔍 API RESPONSE VALIDATION: Verify backend response matches request
  > 838 │   useEffect(() => {
        │   ^^^^^^^^^
    839 │     if (mediaResponse && status === "success") {
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    839 │     if (mediaResponse && status === "success") {
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
  > 841 │       const requestLimit = Number.parseInt(params.get("limit") || "24", 10);
        │                                            ^^^^^^^^^^
    842 │       const pageMatch = requestPage === pagination.page;
    843 │       const limitMatch = requestLimit === pagination.limit;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    838 │   useEffect(() => {
    839 │     if (mediaResponse && status === "success") {
  > 840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
        │                                           ^^^^^^^^^^
    841 │       const requestLimit = Number.parseInt(params.get("limit") || "24", 10);
    842 │       const pageMatch = requestPage === pagination.page;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    881 │ ··},·[mediaResponse,·status,·state.currentPage,·displayAssets.length,·params.get]);
        │                                                                     ++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:838:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on pagination.page.
  
    837 │   // 🔍 API RESPONSE VALIDATION: Verify backend response matches request
  > 838 │   useEffect(() => {
        │   ^^^^^^^^^
    839 │     if (mediaResponse && status === "success") {
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    871 │         "  - Frontend page = Backend page?",
    872 │         frontendMatch ? "✓" : "✗",
  > 873 │         `(${state.currentPage} === ${pagination.page})`,
        │                                      ^^^^^^^^^^^^^^^
    874 │       );
    875 │       if (!pageMatch || !limitMatch || !frontendMatch) {
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    861 │         "  - Request page = Response page?",
    862 │         pageMatch ? "✓" : "✗",
  > 863 │         `(${requestPage} === ${pagination.page})`,
        │                                ^^^^^^^^^^^^^^^
    864 │       );
    865 │       console.log(
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    852 │       console.log("  - frontend currentPage:", state.currentPage);
    853 │       console.log("📥 Response:");
  > 854 │       console.log("  - backend page:", pagination.page);
        │                                        ^^^^^^^^^^^^^^^
    855 │       console.log("  - backend limit:", pagination.limit);
    856 │       console.log("  - backend totalPages:", pagination.totalPages);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    842 │       const pageMatch = requestPage === pagination.page;
    843 │       const limitMatch = requestLimit === pagination.limit;
  > 844 │       const frontendMatch = state.currentPage === pagination.page;
        │                                                   ^^^^^^^^^^^^^^^
    845 │ 
    846 │       console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
    841 │       const requestLimit = Number.parseInt(params.get("limit") || "24", 10);
  > 842 │       const pageMatch = requestPage === pagination.page;
        │                                         ^^^^^^^^^^^^^^^
    843 │       const limitMatch = requestLimit === pagination.limit;
    844 │       const frontendMatch = state.currentPage === pagination.page;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    881 │ ··},·[mediaResponse,·status,·state.currentPage,·displayAssets.length,·pagination.page]);
        │                                                                     +++++++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:838:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on pagination.totalPages.
  
    837 │   // 🔍 API RESPONSE VALIDATION: Verify backend response matches request
  > 838 │   useEffect(() => {
        │   ^^^^^^^^^
    839 │     if (mediaResponse && status === "success") {
    840 │       const requestPage = Number.parseInt(params.get("page") || "1", 10);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    854 │       console.log("  - backend page:", pagination.page);
    855 │       console.log("  - backend limit:", pagination.limit);
  > 856 │       console.log("  - backend totalPages:", pagination.totalPages);
        │                                              ^^^^^^^^^^^^^^^^^^^^^
    857 │       console.log("  - backend total items:", pagination.total);
    858 │       console.log("  - items received:", displayAssets.length);
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    881 │ ··},·[mediaResponse,·status,·state.currentPage,·displayAssets.length,·pagination.totalPages]);
        │                                                                     +++++++++++++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:896:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on setCurrentPage.
  
    895 │   // Reset to page 1 when filters change (important UX)
  > 896 │   useEffect(() => {
        │   ^^^^^^^^^
    897 │     if (state.currentPage > 1) {
    898 │       setCurrentPage(1);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    896 │   useEffect(() => {
    897 │     if (state.currentPage > 1) {
  > 898 │       setCurrentPage(1);
        │       ^^^^^^^^^^^^^^
    899 │     }
    900 │   }, [state.searchTerm, state.selectedType, state.sortBy, state.sortOrder]);
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    900 │ ··},·[state.searchTerm,·state.selectedType,·state.sortBy,·state.sortOrder,·setCurrentPage]);
        │                                                                          ++++++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:896:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on state.currentPage.
  
    895 │   // Reset to page 1 when filters change (important UX)
  > 896 │   useEffect(() => {
        │   ^^^^^^^^^
    897 │     if (state.currentPage > 1) {
    898 │       setCurrentPage(1);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    895 │   // Reset to page 1 when filters change (important UX)
    896 │   useEffect(() => {
  > 897 │     if (state.currentPage > 1) {
        │         ^^^^^^^^^^^^^^^^^
    898 │       setCurrentPage(1);
    899 │     }
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    900 │ ··},·[state.searchTerm,·state.selectedType,·state.sortBy,·state.sortOrder,·state.currentPage]);
        │                                                                          +++++++++++++++++++   

client/src/components/admin/media-library/MediaGrid.tsx:896:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: state.searchTerm, state.selectedType, state.sortBy, state.sortOrder.
  
    895 │   // Reset to page 1 when filters change (important UX)
  > 896 │   useEffect(() => {
        │   ^^^^^^^^^
    897 │     if (state.currentPage > 1) {
    898 │       setCurrentPage(1);
  
  i This dependency can be removed from the list.
  
    898 │       setCurrentPage(1);
    899 │     }
  > 900 │   }, [state.searchTerm, state.selectedType, state.sortBy, state.sortOrder]);
        │       ^^^^^^^^^^^^^^^^
    901 │ 
    902 │   // Asset handling
  
  i This dependency can be removed from the list.
  
    898 │       setCurrentPage(1);
    899 │     }
  > 900 │   }, [state.searchTerm, state.selectedType, state.sortBy, state.sortOrder]);
        │                         ^^^^^^^^^^^^^^^^^^
    901 │ 
    902 │   // Asset handling
  
  i This dependency can be removed from the list.
  
    898 │       setCurrentPage(1);
    899 │     }
  > 900 │   }, [state.searchTerm, state.selectedType, state.sortBy, state.sortOrder]);
        │                                             ^^^^^^^^^^^^
    901 │ 
    902 │   // Asset handling
  
  i This dependency can be removed from the list.
  
    898 │       setCurrentPage(1);
    899 │     }
  > 900 │   }, [state.searchTerm, state.selectedType, state.sortBy, state.sortOrder]);
        │                                                           ^^^^^^^^^^^^^^^
    901 │ 
    902 │   // Asset handling
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    900 │ ··},·[state.searchTerm,·state.selectedType,·state.sortBy,·state.sortOrder]);
        │       -------------------------------------------------------------------   

client/src/components/admin/media-library/MediaLibraryContainerEnhanced.tsx:394:11 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: mediaPickerTarget.
  
    392 │     const { state, updateState } = useMediaLibraryEnhanced();
    393 │ 
  > 394 │     React.useEffect(() => {
        │           ^^^^^^^^^
    395 │       if (initialFilter !== "all" && state.selectedType !== initialFilter) {
    396 │         updateState("selectedType", initialFilter);
  
  i Outer scope values aren't valid dependencies because mutating them doesn't re-render the component.
  
    396 │         updateState("selectedType", initialFilter);
    397 │       }
  > 398 │     }, [initialFilter, mediaPickerTarget, state.selectedType, updateState]);
        │                        ^^^^^^^^^^^^^^^^^
    399 │ 
    400 │     return null;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    398 │ ····},·[initialFilter,·mediaPickerTarget,·state.selectedType,·updateState]);
        │                        -------------------                                  

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:809:60 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    807 │       updates.push({
    808 │         key: "dateRange",
  > 809 │         value: { ...currentState.dateRange, from: new Date(params.get("dateFrom")!) },
        │                                                            ^^^^^^^^^^^^^^^^^^^^^^^
    810 │       });
    811 │     }
  

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:815:58 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    813 │       updates.push({
    814 │         key: "dateRange",
  > 815 │         value: { ...currentState.dateRange, to: new Date(params.get("dateTo")!) },
        │                                                          ^^^^^^^^^^^^^^^^^^^^^
    816 │       });
    817 │     }
  

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:295:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: state.currentPage.
  
    294 │   // Get media data from ALL paginated queries using standardized keys
  > 295 │   useEffect(() => {
        │   ^^^^^^^^^
    296 │     // Use getQueriesData to find ALL media queries across all pages
    297 │     const allMediaQueries = queryClient.getQueriesData({
  
  i This dependency can be removed from the list.
  
    335 │       setAssets([]);
    336 │     }
  > 337 │   }, [queryClient, state.currentPage]); // Re-run when page changes to catch new queries
        │                    ^^^^^^^^^^^^^^^^^
    338 │ 
    339 │   // Debounced search
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    337 │ ··},·[queryClient,·state.currentPage]);·//·Re-run·when·page·changes·to·catch·new·queries
        │                  -------------------                                                    

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:680:24 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: assets.length.
  
    679 │   // PRIORITY 2 FIX: Add updateAssets method for external sync
  > 680 │   const updateAssets = useCallback(
        │                        ^^^^^^^^^^^
    681 │     (newAssets: MediaAsset[]) => {
    682 │       setAssets(newAssets);
  
  i This dependency can be removed from the list.
  
    687 │       }
    688 │     },
  > 689 │     [assets.length, state.selectedAssets.size, clearSelection],
        │      ^^^^^^^^^^^^^
    690 │   );
    691 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    689 │ ····[assets.length,·state.selectedAssets.size,·clearSelection],
        │      ---------------                                           

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:693:24 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on assets.
  
    692 │   // Context value
  > 693 │   const contextValue = useMemo(
        │                        ^^^^^^^
    694 │     () => ({
    695 │       state,
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    720 │       // Missing functions for MediaSelectionWrapperUnified
  > 721 │       assets, // Expose assets for MediaSelectionWrapperUnified
        │       ^^^^^^
    722 │       selectAssets,
    723 │       setSelectedType,
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    747 747 │       }),
    748 748 │       [
    749     │ - ······state,
    750     │ - ······updateState,
    751     │ - ······updateFilter,
    752     │ - ······resetFilters,
    753     │ - ······clearSearch,
    754     │ - ······addTagFilter,
    755     │ - ······removeTagFilter,
    756     │ - ······toggleSortOrder,
    757     │ - ······hasActiveFilters,
    758     │ - ······activeFilterCount,
    759     │ - ······getQueryParams,
    760     │ - ······debouncedSearch,
    761     │ - ······selectionData,
    762     │ - ······toggleAsset,
    763     │ - ······selectAll,
    764     │ - ······clearSelection,
    765     │ - ······isSelected,
    766     │ - ······selectAssets,
    767     │ - ······setSelectedType,
    768     │ - ······uploadFiles,
    769     │ - ······clearUploadProgress,
    770     │ - ······updateAssets,
    771     │ - ······setSelectedAsset,
    772     │ - ······setSelectedAssetIndex,
    773     │ - ······setLightboxOpen,
    774     │ - ······setEditModalOpen,
    775     │ - ······setDeleteModalOpen,
    776     │ - ······setCurrentPage,
    777     │ - ······setTotalPages,
    778     │ - ······setTotalAssets,
    779     │ - ······setErrorState,
        749 │ + ······state,·
        750 │ + ······updateState,·
        751 │ + ······updateFilter,·
        752 │ + ······resetFilters,·
        753 │ + ······clearSearch,·
        754 │ + ······addTagFilter,·
        755 │ + ······removeTagFilter,·
        756 │ + ······toggleSortOrder,·
        757 │ + ······hasActiveFilters,·
        758 │ + ······activeFilterCount,·
        759 │ + ······getQueryParams,·
        760 │ + ······debouncedSearch,·
        761 │ + ······selectionData,·
        762 │ + ······toggleAsset,·
        763 │ + ······selectAll,·
        764 │ + ······clearSelection,·
        765 │ + ······isSelected,·
        766 │ + ······selectAssets,·
        767 │ + ······setSelectedType,·
        768 │ + ······uploadFiles,·
        769 │ + ······clearUploadProgress,·
        770 │ + ······updateAssets,·
        771 │ + ······setSelectedAsset,·
        772 │ + ······setSelectedAssetIndex,·
        773 │ + ······setLightboxOpen,·
        774 │ + ······setEditModalOpen,·
        775 │ + ······setDeleteModalOpen,·
        776 │ + ······setCurrentPage,·
        777 │ + ······setTotalPages,·
        778 │ + ······setTotalAssets,·
        779 │ + ······setErrorState,·//·Missing·functions·for·MediaSelectionWrapperUnified
        780 │ + ······assets
    780 781 │       ],
    781 782 │     );
  

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:693:24 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on setSyncStatus.
  
    692 │   // Context value
  > 693 │   const contextValue = useMemo(
        │                        ^^^^^^^
    694 │     () => ({
    695 │       state,
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    726 │       uploadFiles,
    727 │       clearUploadProgress,
  > 728 │       setSyncStatus,
        │       ^^^^^^^^^^^^^
    729 │ 
    730 │       // PRIORITY 2 FIX: Sync methods
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    747 747 │       }),
    748 748 │       [
    749     │ - ······state,
    750     │ - ······updateState,
    751     │ - ······updateFilter,
    752     │ - ······resetFilters,
    753     │ - ······clearSearch,
    754     │ - ······addTagFilter,
    755     │ - ······removeTagFilter,
    756     │ - ······toggleSortOrder,
    757     │ - ······hasActiveFilters,
    758     │ - ······activeFilterCount,
    759     │ - ······getQueryParams,
    760     │ - ······debouncedSearch,
    761     │ - ······selectionData,
    762     │ - ······toggleAsset,
    763     │ - ······selectAll,
    764     │ - ······clearSelection,
    765     │ - ······isSelected,
    766     │ - ······selectAssets,
    767     │ - ······setSelectedType,
    768     │ - ······uploadFiles,
    769     │ - ······clearUploadProgress,
    770     │ - ······updateAssets,
    771     │ - ······setSelectedAsset,
    772     │ - ······setSelectedAssetIndex,
    773     │ - ······setLightboxOpen,
    774     │ - ······setEditModalOpen,
    775     │ - ······setDeleteModalOpen,
    776     │ - ······setCurrentPage,
    777     │ - ······setTotalPages,
    778     │ - ······setTotalAssets,
    779     │ - ······setErrorState,
        749 │ + ······state,·
        750 │ + ······updateState,·
        751 │ + ······updateFilter,·
        752 │ + ······resetFilters,·
        753 │ + ······clearSearch,·
        754 │ + ······addTagFilter,·
        755 │ + ······removeTagFilter,·
        756 │ + ······toggleSortOrder,·
        757 │ + ······hasActiveFilters,·
        758 │ + ······activeFilterCount,·
        759 │ + ······getQueryParams,·
        760 │ + ······debouncedSearch,·
        761 │ + ······selectionData,·
        762 │ + ······toggleAsset,·
        763 │ + ······selectAll,·
        764 │ + ······clearSelection,·
        765 │ + ······isSelected,·
        766 │ + ······selectAssets,·
        767 │ + ······setSelectedType,·
        768 │ + ······uploadFiles,·
        769 │ + ······clearUploadProgress,·
        770 │ + ······updateAssets,·
        771 │ + ······setSelectedAsset,·
        772 │ + ······setSelectedAssetIndex,·
        773 │ + ······setLightboxOpen,·
        774 │ + ······setEditModalOpen,·
        775 │ + ······setDeleteModalOpen,·
        776 │ + ······setCurrentPage,·
        777 │ + ······setTotalPages,·
        778 │ + ······setTotalAssets,·
        779 │ + ······setErrorState,·setSyncStatus
    780 780 │       ],
    781 781 │     );
  

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:857:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on parseUrlParamsToUpdates.
  
    856 │   // Initialize state from URL on mount
  > 857 │   useEffect(() => {
        │   ^^^^^^^^^
    858 │     const params = new URLSearchParams(globalThis.location.search);
    859 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    860 │     // Parse URL params into state updates
  > 861 │     const updates = parseUrlParamsToUpdates(params, state);
        │                     ^^^^^^^^^^^^^^^^^^^^^^^
    862 │ 
    863 │     if (updates.length > 0) {
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Either include it or remove the dependency array.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    872 │ ··},·[parseUrlParamsToUpdates]);
        │       +++++++++++++++++++++++   

client/src/components/admin/media-library/MediaLibraryContextEnhanced.tsx:857:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on state.
  
    856 │   // Initialize state from URL on mount
  > 857 │   useEffect(() => {
        │   ^^^^^^^^^
    858 │     const params = new URLSearchParams(globalThis.location.search);
    859 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    860 │     // Parse URL params into state updates
  > 861 │     const updates = parseUrlParamsToUpdates(params, state);
        │                                                     ^^^^^
    862 │ 
    863 │     if (updates.length > 0) {
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Either include it or remove the dependency array.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    872 │ ··},·[state]);
        │       +++++   

client/src/components/admin/media-library/MediaUploadEnhanced.tsx:517:30 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on createOptimisticEntries.
  
    516 │   // Process upload queue
  > 517 │   const processUploadQueue = useCallback(async () => {
        │                              ^^^^^^^^^^^
    518 │     if (isUploading) return;
    519 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    526 │     // PHASE 1.2: Create optimistic entries immediately
  > 527 │     createOptimisticEntries(pendingItems);
        │     ^^^^^^^^^^^^^^^^^^^^^^^
    528 │ 
    529 │     try {
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
     702  702 │         setIsUploading(false);
     703  703 │       }
     704      │ - ··},·[uploadQueue,·isUploading,·setSyncStatus,·setErrorState]);
          704 │ + ··},·[uploadQueue,·isUploading,·setSyncStatus,·setErrorState,·//·PHASE·1.2:·Create·optimistic·entries·immediately
          705 │ + ····createOptimisticEntries]);
     705  706 │   
     706  707 │     // CRITICAL FIX: Worker-based upload function to bypass DevTools completely
  

client/src/components/admin/media-library/MediaUploadEnhanced.tsx:517:30 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on uploadLargeFile.
  
    516 │   // Process upload queue
  > 517 │   const processUploadQueue = useCallback(async () => {
        │                              ^^^^^^^^^^^
    518 │     if (isUploading) return;
    519 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    556 │                 if (item.file.size > CHUNK_THRESHOLD) {
    557 │                   // Use chunked upload for large files (handles its own validation and progress)
  > 558 │                   await uploadLargeFile(item, controller, setUploadQueue);
        │                         ^^^^^^^^^^^^^^^
    559 │                 } else {
    560 │                   // PHASE 1.1 FIX: Use apiRequest for regular uploads (fixed FormData handling)
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    704 │ ··},·[uploadQueue,·isUploading,·setSyncStatus,·setErrorState,·uploadLargeFile]);
        │                                                             +++++++++++++++++   

client/src/components/admin/media-library/MediaUploadEnhanced.tsx:966:26 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: setIsDragging.
  
    965 │   // Drag and drop handlers
  > 966 │   const handleDragOver = useCallback(
        │                          ^^^^^^^^^^^
    967 │     (e: React.DragEvent<HTMLDivElement>): void => {
    968 │       e.preventDefault();
  
  i This dependency can be removed from the list.
  
    969 │       setIsDragging(true);
    970 │     },
  > 971 │     [setIsDragging],
        │      ^^^^^^^^^^^^^
    972 │   );
    973 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    971 │ ····[setIsDragging],
        │      -------------  

client/src/components/admin/media-library/MediaUploadEnhanced.tsx:974:27 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: setIsDragging.
  
    972 │   );
    973 │ 
  > 974 │   const handleDragLeave = useCallback(
        │                           ^^^^^^^^^^^
    975 │     (e: React.DragEvent<HTMLDivElement>): void => {
    976 │       e.preventDefault();
  
  i This dependency can be removed from the list.
  
    977 │       setIsDragging(false);
    978 │     },
  > 979 │     [setIsDragging],
        │      ^^^^^^^^^^^^^
    980 │   );
    981 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    979 │ ····[setIsDragging],
        │      -------------  

client/src/components/admin/media-library/MediaUploadEnhanced.tsx:982:22 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: setIsDragging.
  
    980 │   );
    981 │ 
  > 982 │   const handleDrop = useCallback(
        │                      ^^^^^^^^^^^
    983 │     (e: React.DragEvent<HTMLDivElement>): void => {
    984 │       e.preventDefault();
  
  i This dependency can be removed from the list.
  
    990 │       }
    991 │     },
  > 992 │     [handleFileSelect, setIsDragging],
        │                        ^^^^^^^^^^^^^
    993 │   );
    994 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    992 │ ····[handleFileSelect,·setIsDragging],
        │                      ---------------  

client/src/components/admin/navigation/NavigationForm.tsx:367:30 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    365 │         mediaPickerTarget="navigation-icon"
    366 │         selectionMode="single"
  > 367 │         initialSelectedIds={[formData.mediaIconId!].filter(Boolean)}
        │                              ^^^^^^^^^^^^^^^^^^^^^
    368 │       />
    369 │     </>
  

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:694:19 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    693 │       // Category & Fabric
  > 694 │       categoryId: formData.categoryId!,
        │                   ^^^^^^^^^^^^^^^^^^^^
    695 │       fabricId: formData.fabricId,
    696 │       sizeChartId: formData.sizeChartId,
  

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:371:28 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━

  ! This hook does not specify its dependency on validateField.
  
    370 │   // Pure validation function - no side effects, properly memoized
  > 371 │   const validateFormPure = useCallback((data: typeof formData) => {
        │                            ^^^^^^^^^^^
    372 │     const errors: Record<string, string> = {};
    373 │     let errorCount = 0;
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    389 │     fieldsToValidate.forEach((field) => {
  > 390 │       const error = validateField(field, data[field as keyof typeof data]);
        │                     ^^^^^^^^^^^^^
    391 │       if (error) {
    392 │         errors[field] = error;
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Either include it or remove the dependency array.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    419 │ ··},·[validateField]);·//·No·dependencies·-·pure·function
        │       +++++++++++++                                      

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:487:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on autoSaveTimer.
  
    486 │   // Auto-save draft functionality
  > 487 │   useEffect(() => {
        │   ^^^^^^^^^
    488 │     if (autoSaveTimer) {
    489 │       clearTimeout(autoSaveTimer);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    486 │   // Auto-save draft functionality
    487 │   useEffect(() => {
  > 488 │     if (autoSaveTimer) {
        │         ^^^^^^^^^^^^^
    489 │       clearTimeout(autoSaveTimer);
    490 │     }
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    487 │   useEffect(() => {
    488 │     if (autoSaveTimer) {
  > 489 │       clearTimeout(autoSaveTimer);
        │                    ^^^^^^^^^^^^^
    490 │     }
    491 │ 
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    502 │     return () => {
  > 503 │       if (autoSaveTimer) {
        │           ^^^^^^^^^^^^^
    504 │         clearTimeout(autoSaveTimer);
    505 │       }
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    502 │     return () => {
    503 │       if (autoSaveTimer) {
  > 504 │         clearTimeout(autoSaveTimer);
        │                      ^^^^^^^^^^^^^
    505 │       }
    506 │     };
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    507 │ ··},·[formData,·isLoading,·autoSaveTimer]);
        │                          +++++++++++++++   

client/src/components/admin/product-management-unified/admin/ProductCreateEditModal.tsx:510:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on formData.
  
    509 │   // Phase 3: Workflow Enhancement - Keyboard Shortcuts & Quick Actions
  > 510 │   useEffect(() => {
        │   ^^^^^^^^^
    511 │     const handleKeyDown = (e: KeyboardEvent) => {
    512 │       // Ctrl/Cmd + S for save
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    521 │         // Reset form for duplication
    522 │         updateMultipleFields({
  > 523 │           ...formData,
        │              ^^^^^^^^
    524 │           name: `${formData.name} (Copy)`,
    525 │           sku: "",
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    543 │ ··},·[isOpen,·isLoading,·isEditing,·onClose,·formData]);
        │                                            ++++++++++   

client/src/components/admin/product-management-unified/advanced/ProductAdvancedFilters.tsx:58:37 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━

  ! This hook does not specify its dependency on searchFilteredProducts.filter.
  
    57 │   // Phase 2/3: Advanced Features - Complex Filtering System with debounced search
  > 58 │   const filteredAndSortedProducts = useMemo(() => {
       │                                     ^^^^^^^
    59 │     // Start with search-filtered products from the debounced hook
    60 │     const filtered = searchFilteredProducts.filter((product) => {
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    58 │   const filteredAndSortedProducts = useMemo(() => {
    59 │     // Start with search-filtered products from the debounced hook
  > 60 │     const filtered = searchFilteredProducts.filter((product) => {
       │                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    61 │       // Status filter
    62 │       const matchesStatus =
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    130 130 │       return filtered;
    131 131 │     }, [
    132     │ - ····products,
    133     │ - ····categories,
    134     │ - ····fabrics,
    135     │ - ····searchQuery,
    136     │ - ····statusFilter,
    137     │ - ····categoryFilter,
    138     │ - ····fabricFilter,
    139     │ - ····featuredFilter,
    140     │ - ····sortBy,
    141     │ - ····sortOrder,
        132 │ + ····products,·
        133 │ + ····categories,·
        134 │ + ····fabrics,·
        135 │ + ····searchQuery,·
        136 │ + ····statusFilter,·
        137 │ + ····categoryFilter,·
        138 │ + ····fabricFilter,·
        139 │ + ····featuredFilter,·
        140 │ + ····sortBy,·
        141 │ + ····sortOrder,·searchFilteredProducts.filter
    142 142 │     ]);
    143 143 │   
  

client/src/components/admin/product-management-unified/advanced/ProductAdvancedFilters.tsx:58:37 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: products, searchQuery.
  
    57 │   // Phase 2/3: Advanced Features - Complex Filtering System with debounced search
  > 58 │   const filteredAndSortedProducts = useMemo(() => {
       │                                     ^^^^^^^
    59 │     // Start with search-filtered products from the debounced hook
    60 │     const filtered = searchFilteredProducts.filter((product) => {
  
  i Outer scope values aren't valid dependencies because mutating them doesn't re-render the component.
  
    130 │     return filtered;
    131 │   }, [
  > 132 │     products,
        │     ^^^^^^^^
    133 │     categories,
    134 │     fabrics,
  
  i This dependency can be removed from the list.
  
    133 │     categories,
    134 │     fabrics,
  > 135 │     searchQuery,
        │     ^^^^^^^^^^^
    136 │     statusFilter,
    137 │     categoryFilter,
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    130 130 │       return filtered;
    131 131 │     }, [
    132     │ - ····products,
    133     │ - ····categories,
    134     │ - ····fabrics,
    135     │ - ····searchQuery,
    136     │ - ····statusFilter,
    137     │ - ····categoryFilter,
    138     │ - ····fabricFilter,
    139     │ - ····featuredFilter,
    140     │ - ····sortBy,
    141     │ - ····sortOrder,
        132 │ + ····categories,·
        133 │ + ····fabrics,·
        134 │ + ····statusFilter,·
        135 │ + ····categoryFilter,·
        136 │ + ····fabricFilter,·
        137 │ + ····featuredFilter,·
        138 │ + ····sortBy,·
        139 │ + ····sortOrder
    142 140 │     ]);
    143 141 │   
  

client/src/components/admin/product-management-unified/advanced/ProductAdvancedFilters.tsx:145:9 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━

  ! This hook does not specify its dependency on filteredAndSortedProducts.
  
    144 │   // Update filtered products when filters change
  > 145 │   React.useEffect(() => {
        │         ^^^^^^^^^
    146 │     onFilteredProductsChange(filteredAndSortedProducts);
    147 │   }, [
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    144 │   // Update filtered products when filters change
    145 │   React.useEffect(() => {
  > 146 │     onFilteredProductsChange(filteredAndSortedProducts);
        │                              ^^^^^^^^^^^^^^^^^^^^^^^^^
    147 │   }, [
    148 │     searchFilteredProducts,
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    146 146 │       onFilteredProductsChange(filteredAndSortedProducts);
    147 147 │     }, [
    148     │ - ····searchFilteredProducts,
    149     │ - ····statusFilter,
    150     │ - ····categoryFilter,
    151     │ - ····fabricFilter,
    152     │ - ····featuredFilter,
    153     │ - ····sortBy,
    154     │ - ····sortOrder,
    155     │ - ····onFilteredProductsChange,
        148 │ + ····searchFilteredProducts,·
        149 │ + ····statusFilter,·
        150 │ + ····categoryFilter,·
        151 │ + ····fabricFilter,·
        152 │ + ····featuredFilter,·
        153 │ + ····sortBy,·
        154 │ + ····sortOrder,·
        155 │ + ····onFilteredProductsChange,·filteredAndSortedProducts
    156 156 │     ]);
    157 157 │   
  

client/src/components/admin/product-management-unified/advanced/ProductAdvancedFilters.tsx:145:9 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: searchFilteredProducts, statusFilter, categoryFilter, fabricFilter, featuredFilter, sortBy, sortOrder.
  
    144 │   // Update filtered products when filters change
  > 145 │   React.useEffect(() => {
        │         ^^^^^^^^^
    146 │     onFilteredProductsChange(filteredAndSortedProducts);
    147 │   }, [
  
  i This dependency can be removed from the list.
  
    146 │     onFilteredProductsChange(filteredAndSortedProducts);
    147 │   }, [
  > 148 │     searchFilteredProducts,
        │     ^^^^^^^^^^^^^^^^^^^^^^
    149 │     statusFilter,
    150 │     categoryFilter,
  
  i This dependency can be removed from the list.
  
    147 │   }, [
    148 │     searchFilteredProducts,
  > 149 │     statusFilter,
        │     ^^^^^^^^^^^^
    150 │     categoryFilter,
    151 │     fabricFilter,
  
  i This dependency can be removed from the list.
  
    148 │     searchFilteredProducts,
    149 │     statusFilter,
  > 150 │     categoryFilter,
        │     ^^^^^^^^^^^^^^
    151 │     fabricFilter,
    152 │     featuredFilter,
  
  i This dependency can be removed from the list.
  
    149 │     statusFilter,
    150 │     categoryFilter,
  > 151 │     fabricFilter,
        │     ^^^^^^^^^^^^
    152 │     featuredFilter,
    153 │     sortBy,
  
  i This dependency can be removed from the list.
  
    150 │     categoryFilter,
    151 │     fabricFilter,
  > 152 │     featuredFilter,
        │     ^^^^^^^^^^^^^^
    153 │     sortBy,
    154 │     sortOrder,
  
  i This dependency can be removed from the list.
  
    151 │     fabricFilter,
    152 │     featuredFilter,
  > 153 │     sortBy,
        │     ^^^^^^
    154 │     sortOrder,
    155 │     onFilteredProductsChange,
  
  i This dependency can be removed from the list.
  
    152 │     featuredFilter,
    153 │     sortBy,
  > 154 │     sortOrder,
        │     ^^^^^^^^^
    155 │     onFilteredProductsChange,
    156 │   ]);
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    146 146 │       onFilteredProductsChange(filteredAndSortedProducts);
    147 147 │     }, [
    148     │ - ····searchFilteredProducts,
    149     │ - ····statusFilter,
    150     │ - ····categoryFilter,
    151     │ - ····fabricFilter,
    152     │ - ····featuredFilter,
    153     │ - ····sortBy,
    154     │ - ····sortOrder,
    155     │ - ····onFilteredProductsChange,
        148 │ + ····onFilteredProductsChange
    156 149 │     ]);
    157 150 │   
  

client/src/components/admin/product-management-unified/core/ProductGrid.tsx:66:29 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: selectedProductIds, setSelectedProductIds.
  
    65 │   // Render individual product item
  > 66 │   const renderProductCard = useCallback(
       │                             ^^^^^^^^^^^
    67 │     (product: Product) => {
    68 │       return (
  
  i Outer scope values aren't valid dependencies because mutating them doesn't re-render the component.
  
    89 │       onProductEdit,
    90 │       handleProductDelete,
  > 91 │       selectedProductIds,
       │       ^^^^^^^^^^^^^^^^^^
    92 │       setSelectedProductIds,
    93 │     ],
  
  i Outer scope values aren't valid dependencies because mutating them doesn't re-render the component.
  
    90 │       handleProductDelete,
    91 │       selectedProductIds,
  > 92 │       setSelectedProductIds,
       │       ^^^^^^^^^^^^^^^^^^^^^
    93 │     ],
    94 │   );
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
     82  82 │       },
     83  83 │       [
     84     │ - ······getCategory,
     85     │ - ······getFabric,
     86     │ - ······getMediaAsset,
     87     │ - ······viewMode,
     88     │ - ······onProductSelect,
     89     │ - ······onProductEdit,
     90     │ - ······handleProductDelete,
     91     │ - ······selectedProductIds,
     92     │ - ······setSelectedProductIds,
         84 │ + ······getCategory,·
         85 │ + ······getFabric,·
         86 │ + ······getMediaAsset,·
         87 │ + ······viewMode,·
         88 │ + ······onProductSelect,·
         89 │ + ······onProductEdit,·
         90 │ + ······handleProductDelete
     93  91 │       ],
     94  92 │     );
  

client/src/components/admin/product-management-unified/sections/MediaAssetsSection.tsx:451:61 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    449 │                       <button
    450 │                         type="button"
  > 451 │                         onClick={() => removeMedia("model", formData.modelFileId!)}
        │                                                             ^^^^^^^^^^^^^^^^^^^^^
    452 │                         className="absolute top-2 right-2 p-1.5 bg-red-500 text-white rounded-full opacity-0 group-hover:opacity-100 transition-opacity shadow-lg"
    453 │                       >
  

client/src/components/admin/product-management-unified/shared/hooks/useDebouncedSearch.ts:37:14 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    35 │     if (searchCache.has(cacheKey)) {
    36 │       logger.debug("Search cache hit", { query: debouncedQuery });
  > 37 │       return searchCache.get(cacheKey)!;
       │              ^^^^^^^^^^^^^^^^^^^^^^^^^^
    38 │     }
    39 │ 
  

client/src/components/admin/shared/MediaSelectionWrapperUnified.tsx:259:53 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    258 │       if (foundAssets.length > 0) {
  > 259 │         const result = selectionMode === "single" ? foundAssets[0]! : foundAssets;
        │                                                     ^^^^^^^^^^^^^^^
    260 │ 
    261 │         console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
  

client/src/components/admin/unified-sustainability-management.tsx:307:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on getTabFromUrl.
  
    306 │   // Listen to popstate (back/forward) to sync tab
  > 307 │   useEffect(() => {
        │   ^^^^^^^^^
    308 │     const handlePopState = () => {
    309 │       setActiveTab(getTabFromUrl());
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    307 │   useEffect(() => {
    308 │     const handlePopState = () => {
  > 309 │       setActiveTab(getTabFromUrl());
        │                    ^^^^^^^^^^^^^
    310 │     };
    311 │     window.addEventListener("popstate", handlePopState);
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Either include it or remove the dependency array.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    313 │ ··},·[getTabFromUrl]);
        │       +++++++++++++   

client/src/components/admin/unified-sustainability-management.tsx:377:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook does not specify its dependency on resetForm.
  
    375 │   };
    376 │ 
  > 377 │   useEffect(() => {
        │   ^^^^^^^^^
    378 │     resetForm();
    379 │   }, [unifiedData]);
  
  i This dependency is being used here, but is not specified in the hook dependency list.
  
    377 │   useEffect(() => {
  > 378 │     resetForm();
        │     ^^^^^^^^^
    379 │   }, [unifiedData]);
    380 │ 
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Failing to specify dependencies can result in Effects not updating correctly when state changes.
    These "stale closures" are a common source of surprising bugs.
  
  i Unsafe fix: Add the missing dependency to the list.
  
    379 │ ··},·[unifiedData,·resetForm]);
        │                  +++++++++++   

client/src/components/admin/unified-sustainability-management.tsx:377:3 lint/correctness/useExhaustiveDependencies  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! This hook specifies more dependencies than necessary: unifiedData.
  
    375 │   };
    376 │ 
  > 377 │   useEffect(() => {
        │   ^^^^^^^^^
    378 │     resetForm();
    379 │   }, [unifiedData]);
  
  i This dependency can be removed from the list.
  
    377 │   useEffect(() => {
    378 │     resetForm();
  > 379 │   }, [unifiedData]);
        │       ^^^^^^^^^^^
    380 │ 
    381 │   // Use mutations hook
  
  i React relies on hook dependencies to determine when to re-compute Effects.
    Specifying more dependencies than required can lead to unnecessary re-rendering
    and degraded performance.
  
  i Unsafe fix: Remove the extra dependencies from the list.
  
    379 │ ··},·[unifiedData]);
        │       -----------   

tests/integration/test-utils.ts:38:26 lint/style/noNonNullAssertion ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ! Forbidden non-null assertion.
  
    36 │         if (serverProcess.exitCode === null) {
    37 │           try {
  > 38 │             process.kill(serverProcess.pid!, "SIGKILL");
       │                          ^^^^^^^^^^^^^^^^^^
    39 │           } catch (e) {
    40 │             /* ignore if already gone */
  

client/src/components/GradientBlinds.tsx:676:9 lint/a11y/useAriaPropsSupportedByRole ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × The ARIA attribute 'aria-label' is not supported by this element.
  
    674 │       <div className={`gradient-blinds-container gradient-blinds-fallback ${className || ""}`}>
    675 │         {/* CSS Fallback with current admin settings */}
  > 676 │         <div
        │         ^^^^
  > 677 │           className="gradient-blinds-css-fallback"
  > 678 │           style={cssCustomProps}
  > 679 │           aria-label="Gradient background (static preview)"
  > 680 │         />
        │         ^^
    681 │ 
    682 │         {/* User feedback for fallback mode */}
  
  i Ensure that ARIA attributes are valid for the role of the element.
  

client/src/components/admin/media-library/MediaUploadEnhanced.tsx:1040:9 lint/a11y/noAriaHiddenOnFocusable  FIXABLE  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Disallow aria-hidden="true" from being set on focusable elements.
  
    1038 │         }}
    1039 │       >
  > 1040 │         <input
         │         ^^^^^^
  > 1041 │           ref={fileInputRef}
          ...
  > 1049 │           aria-hidden="true"
  > 1050 │         />
         │         ^^
    1051 │ 
    1052 │         <div className="flex flex-col items-center gap-2">
  
  i aria-hidden should not be set to true on focusable elements because this can lead to confusing behavior for screen reader users.
  
  i Unsafe fix: Remove the aria-hidden attribute from the element.
  
    1047 1047 │             id="media-upload-file-input"
    1048 1048 │             aria-label="Upload media files"
    1049      │ - ··········aria-hidden="true"
    1050 1049 │           />
    1051 1050 │   
  

client/src/components/admin/product-management-unified/advanced/ProductAdvancedFilters.tsx:222:15 lint/a11y/useAriaPropsSupportedByRole ━━━━━━━━━━━━━━━━━━━━━━━━━

  × The ARIA attribute 'aria-label' is not supported by this element.
  
    220 │           {isSearching && (
    221 │             <div className="absolute right-3 top-1/2 transform -translate-y-1/2">
  > 222 │               <div
        │               ^^^^
  > 223 │                 className="animate-spin rounded-full h-4 w-4 border-b-2 border-blue-600"
  > 224 │                 aria-label="Searching..."
  > 225 │               ></div>
        │               ^
    226 │             </div>
    227 │           )}
  
  i Ensure that ARIA attributes are valid for the role of the element.
  

client/src/components/admin/technology/TechnologyGradientSettings.tsx:77:17 lint/suspicious/noRedeclare ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Shouldn't redeclare 'TechnologyGradientSettings'. Consider to delete it or rename it.
  
    75 │ }
    76 │ 
  > 77 │ export function TechnologyGradientSettings({
       │                 ^^^^^^^^^^^^^^^^^^^^^^^^^^
    78 │   gradientData,
    79 │   setGradientData,
  
  i 'TechnologyGradientSettings' is defined here:
  
    12 │  */
    13 │ 
  > 14 │ import type { TechnologyGradientSettings } from "@shared/schema";
       │               ^^^^^^^^^^^^^^^^^^^^^^^^^^
    15 │ import { useMutation } from "@tanstack/react-query";
    16 │ import { ChevronDown, ChevronUp } from "lucide-react";
  

The number of diagnostics exceeds the limit allowed. Use --max-diagnostics to increase it.
Diagnostics not shown: 1906.
Checked 1023 files in 9s. No fixes applied.
Found 1134 errors.
Found 765 warnings.
Found 102 infos.
check ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Some errors were emitted while running checks.
  

